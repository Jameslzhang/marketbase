# A股T+1全生命周期统一主规范（整合设计稿）

版本：设计评审稿 V1.6  
日期：2026-07-30  
状态：实施顺序与基础设施门槛补齐；行情采集频率待接口容量评估；本稿不自动下单

## 0. 文档权威性、整合范围与依赖索引

### 0.1 单一权威主规范

本文件是统一主规范的整合目标，最终同时覆盖固定自选池与全市场T+1，但两者作为独立策略配置运行。

在本文件完成公式内嵌、双实现一致性测试、历史回放和影子验收，并冻结 `effective_from` 之前，它仍是设计评审稿，不反向覆盖已经生效的生产版本。完成原子切换后，本文件及其同文件附录成为唯一生产规则源；旧设计稿、落地报告和审计报告只保留为来源记录，不再拥有规则解释权。

统一主规范不得只写“继承某合同”而省略关键定义。凡是会影响候选晋级、买卖价格、仓位、状态转换或审计标签的概念，必须在本文件正文或同文件附录中给出：

- 唯一公式、单位、输入窗口和价格取整方向；
- 数据来源、新鲜度和缺失处理；
- 状态前置条件、输出权限和原因码；
- 参数版本、校准状态和回滚条件；
- 对应的正向、反向及边界测试。

外部文件可以作为来源证据，但实施者不应依赖跨文件阅读才能解释生产决策。

### 0.2 双策略配置

共享生命周期合同之上冻结两个互不串用候选和模型状态的配置：

| 配置 | `strategy_profile_id` | 股票范围 | 通道集合 | 候选与审计边界 |
|---|---|---|---|---|
| 固定自选池T+1 | `watchlist_t1_v1` | 每日09:25前冻结的固定自选池；初始为既有23只 | `trend_continuation`、`strong_pullback_reclaim`、`sector_reversal_challenger`；反转通道未晋级前保持影子 | 独立候选名额、状态账本、模型版本和命中统计 |
| 全市场T+1 | `full_market_t1_v1` | 通过证券资格与数据完整性过滤的沪深A股冻结宇宙 | `trend_continuation`、`strong_pullback_reclaim`；`high_momentum`与`sector_reversal_challenger`在各自完整合同验收前仅影子 | 独立候选名额、状态账本、模型版本和命中统计 |

两个配置共享以下确定性合同，但不得共享当日临时候选、评分、概率、状态或样本分母：

- 标准行情、分钟、日线、行业和证券元数据Schema；
- 价格取整、费用、压力损失和账户快照；
- 买区、禁追价、保护位、卖区和正常可成交代理；
- 市场环境、行业证据、数据异常和Provider切换；
- 入场、持仓、退出、对账、审计和版本迁移状态机。

每个决策、计划、持仓和审计记录必须同时保存 `strategy_profile_id` 与 `strategy_version`。缺少配置ID、跨配置读取候选、或把同一交易同时记入两个配置的生产指标，均返回 `strategy_profile_isolation_violation`。

### 0.3 现有策略文件的并入顺序

统一主规范从以下已存在文件吸收规则；“来源章节”只用于整合追溯，最终公式必须写入本文件：

| 来源文件 | 本次吸收内容 | 整合后的地位 |
|---|---|---|
| `D:\Codex\02_正式交付区\V07231155_自选池纯短线策略生产规范第5版.md` | 固定自选池、三通道、数据合同、买区与禁追价、保护位与卖区、费用压力、市场环境、可成交代理、账户风险、状态和审计公式 | 主要规则来源；整合完成后转为历史基线 |
| `D:\Users\Documents\脚本\resume-hunter\V07301026_A股全生命周期策略优化设计.md` | 双轴判断、完整入场与持仓生命周期、利润保护、T+2以后、对账和版本回退 | 当前整合目标 |
| `D:\Codex\02_正式交付区\V07241414_自选池次日上涨影子算法落地说明.md` | 影子特征、评分试验和晋级证据 | 仅研究来源；未达校准门槛的数值不得进入生产 |
| `D:\Codex\02_正式交付区\V07231615_自选池纯短线生产落地报告.md` | 已实现能力、运行入口和验证证据 | 仅实施证据，不定义策略 |

发生冲突时按以下顺序处理，而不是选择较新的自然语言描述：

1. 先区分配置特有规则和共享合同，禁止把全市场四通道直接写入固定自选池。
2. 再比较合同版本、证据时间和是否通过生产验收。
3. 已冻结确定性公式优先于未校准影子参数；生命周期结构修复通过新合同版本显式替换旧结构。
4. 无法唯一裁决时标记 `contract_conflict_pending`，在解决并补充迁移测试前不得进入生产。

### 0.4 本地事实层与模型判断层

行情准确性与模型判断准确性分离：

- 本地 `ObjectiveDataProvider` 是价格、分钟线、日线、行业、证券状态和账户快照的唯一事实入口。
- VWAP、ATR、结构位、买卖区、费用、压力损失、状态转换和审计标签由可复现的确定性程序计算。
- 模型端只读取带 `as_of`、`available_at`、Provider、公式版本、证据ID和原始响应哈希的冻结结构化快照，用于解释、排序和生成用户可读计划。
- 模型不得自行补价格、在冲突来源中选择“看起来正确”的值，或用盘后数据回写盘中结论。

行情“采集频率”与“决策数据新鲜度”是两个不同合同。考虑公开接口限流和封禁风险，本稿不冻结“全市场每分钟采集”：

```text
COLLECTION_CADENCE_V1.status = deferred_capacity_review
full_market_per_minute_polling_required = false
```

后续必须先用接口调用上限、单次批量容量、失败率、封禁风险、数据回补能力和本地存储压力完成容量评估，再单独冻结采集频率。尚未冻结采集频率不允许被解释为可以使用旧数据；每个实际决策仍必须满足对应字段的新鲜度、完整性和可复现要求。采集合同未通过容量测试前，相关配置不得标记生产就绪。

## 1. 目标

本次优化不是继续收紧买入门槛，而是把现有“选出候选—给出买区”的策略升级为覆盖选股、入场、T+1锁定、次日开盘、冲高失败、风险退出和审计的全生命周期策略。

目标同时满足：

1. 保留正常的候选覆盖和出手机会，不通过层层否决制造长期空仓。
2. 条件观察不能被表达成可直接成交的正式买点。
3. 买入后必须持续管理，不允许静态止损失守后仍沿用原推荐逻辑。
4. 把“选错股票、买错位置、退出过晚、无法成交”分开审计。
5. 数值参数必须通过历史回放和影子样本校准，不能由杰瑞股份、中科曙光两笔结果直接拍脑袋修改。

## 2. 失败证据与根因

### 2.1 杰瑞股份

- 2026-07-29约13:59以138.80元买入100股。
- 推荐前的公开分析已承认油服行业同步偏弱、股票属于修复而非完整主升。
- 对话曾展示138.80–140.00买入区，但没有持久化满足生产要求的连续三根完成分钟确认。
- 随后的正式复核又给出空仓并将杰瑞股份降为观察，说明“条件候选”和“正式可执行状态”在对话层发生状态泄漏。
- 次日开盘140.71元，最高142.00元，相对成本一度盈利约2.31%；原第一卖区从142.45元开始，仅差0.45元。
- 股票随后快速跌至成本以下。策略缺少“接近目标后回落保护”和“高开强势失败退出”，把一次可小盈或小亏结束的交易拖成明显亏损。

结论：杰瑞股份同时暴露了入场状态绕过和次日退出路径缺失，不应简单归因于选股阈值偏松。

### 2.2 中科曙光

既有审计已确认：

- 买区上沿104.30元的目标二收益风险比仅0.76，低于当时最低要求0.80；
- 高贝塔成长股入场时，相关成长指数已经明显走弱且继续恶化；
- 原推荐时间戳和不可变候选快照缺失，导致完整可执行性无法进入正式统计；
- 后续原执行止损100.50元失守后，持仓没有强制转入风险退出状态，仍长期以等待反弹的方式管理。

结论：中科曙光的入口筛选问题已有部分修复，但持仓生命周期和状态持久化仍未解决。

### 2.3 共同根因

1. **状态泄漏**：研究候选、条件观察和正式可买在自然语言输出中没有形成不可绕过的边界。
2. **人工旁路**：生产引擎已有连续分钟确认和状态机，但对话中的临时分析可以绕过生产状态直接给出买区。
3. **止损静态化**：止损只是一个价格字段，没有成为持续更新、不可逆转的持仓状态。
4. **目标刚性**：只等待固定第一卖区，未处理“接近目标后回落”“高开失败”“冲高失速”。
5. **尾部风险未独立计量**：机会评分可以掩盖隔夜跳空、日内高波动和一手不可拆分风险。
6. **证据持久化不足**：推荐时间、分钟确认、状态转换和次日退出证据不完整，造成事后无法准确归因。

## 3. 总体设计

策略改为“六层流水线”：

```text
完整候选覆盖
  ↓
独立通道识别
  ↓
机会质量 + 尾部风险双轴判断
  ↓
正式入场状态机
  ↓
持仓与次日退出状态机
  ↓
分阶段审计和回滚
```

### 3.1 候选通道

保留并隔离现有通道，不通过放松单一阈值增加机会：

1. `trend_continuation`：日线和盘中趋势延续。
2. `strong_pullback_reclaim`：强势股回踩后收复VWAP与命名枢轴。
3. `high_momentum`：高流动性强势股的正常可成交延续。
4. `sector_reversal_challenger`：板块第一日反转观察；证据未达到生产门槛前保持影子状态。

各通道独立产生领先者。同一股票只能指定一个主通道，不能拼接不同通道的局部优点形成买点。

通道集合由 `strategy_profile_id + strategy_version` 联合冻结：`full_market_t1_v1` 保留四个通道槽位，但当前只有趋势延续和强势回踩可以生产晋级，高动量与板块反转按各自合同状态保持影子；`watchlist_t1_v1` 继续沿用其三个通道槽位，板块反转未晋级前同样只影子。生命周期合同不自动把 `high_momentum` 写入固定自选池。

### 3.2 双轴判断

不再允许一个总分掩盖风险：

- `opportunity_quality`：行业同步按附录A.2 `industry_sync_pass`，VWAP位置按附录A.1 `vwap_position_pass`，分钟延续按附录A.3 `ACTIVITY_HOLD_V1`，目标空间和手续费后经济性按附录A.4 `ECONOMICS_V1`，日线结构与通道条件必须有版本化硬规则。
- `tail_risk`：ATR、隔夜跳空、上影和行业拥挤按附录A.5，涨跌停压力、账户集中度和相关持仓压力按正文 `ACCOUNT_RISK_V1`；一手不可拆分按§5.6。

机会质量用于排序，尾部风险用于决定是否可执行及仓位形态。两者不得相互抵消。

机会质量分为：

- `high`：主通道全部背景和执行硬条件通过；`first_zone_net_reward>0`、`reward_risk_ratio>=1.20`；行业同步、VWAP位置和3分钟活动度全部通过；
- `medium`：主通道背景成立，但分钟确认、行业同步或经济性仍有一项待确认；
- `low`：主通道背景硬条件失败，或买区、保护位、正常可成交窗口无法构造。

尾部风险分为：

- `low`：市场否决未触发，账户压力预算、单笔压力和相关持仓预算全部通过，无高波动或一手不可拆分警报；
- `medium`：没有突破任何硬预算，但附录A.5的 `high_atr_warning`、`long_upper_shadow`、`industry_crowding_warning`、`overnight_gap_warning` 或一手不可拆分至少一项成立；必须降低可等待时间或限制为最小可交易单位；
- `high`：市场硬否决触发、最小100股突破风险预算、保护位不可构造、正常退出窗口不可验证，或数据不足导致尾部压力无法计算。

双轴决策矩阵固定如下：

| 机会质量 | 尾部风险低 | 尾部风险中 | 尾部风险高 |
|---|---|---|---|
| 高 | 可进入正式候选比较 | `ACCOUNT_RISK_V1` 允许100股最小单位时可进入正式候选比较；否则条件观察 | 不可买；保留风险观察或明确拒绝 |
| 中 | 条件观察，等待缺失证据 | 条件观察或深度观察，不可发布买点 | 不可买 |
| 低 | 拒绝当日入场 | 拒绝当日入场 | 拒绝当日入场 |

`high/medium/low` 是硬条件结果的枚举，不是把各字段重新加权后的总分。禁止用高机会分覆盖高尾部风险，也禁止用低尾部风险把低质量机会抬升为候选。

矩阵中的“账户风险允许”不得由模型主观判断。本文 `ACCOUNT_RISK_V1` 固定如下：

- 只有 `account_aware` 模式且 `ACCOUNT_SNAPSHOT_V1` 完整、新鲜、当日无后续未纳入交易时，才允许判断具体股数；否则为 `plan_only / sizing_status=unresolved`，该分支最多保持条件观察，不得声称最小单位可买。
- 枚举100股档位时，普通趋势延续或强势回踩资金占用不超过账户权益5%，当日涨幅7%以上的高动量机会不超过3%。
- 计划损失不超过账户权益0.25%，次日跌停压力不超过0.50%，多日无法退出压力不超过0.90%。
- 纳入已有持仓、待成交买单和新候选后，隔夜总压力不超过账户权益1.00%；同一主行业或冻结相关主题的压力合计不超过0.75%、市值合计不超过15%。
- 任一限制在100股档位失败即 `minimum_lot_risk_exceeded` 或对应风险原因码，不得通过减少到不足一手、忽略已有持仓或语言判断豁免。
- 以上是账户风险上限，不是预测参数；只能由用户明确设得更低，提高上限必须另行确认并生成新风险合同版本。

`ACCOUNT_SNAPSHOT_V1` 至少包含快照ID、当日时间、账户权益、可用现金、每只持仓的代码/总数量/可售数量/成本/冻结数量/主行业/相关组，以及待成交委托和“快照后无变化”的用户确认。快照必须属于当前交易日、在计划发布前有效；任一持仓价格、数量、可售状态、行业或后续交易不明时降级 `plan_only`。

三层压力统一为：`planned_loss` 使用附录A.4买区上沿至保护位的费用后损失；`next_day_limit_stress` 使用买入交易日官方收盘或交易所参考价计算的次日理论跌停退出损失；`multi_day_exit_unavailable_stress` 使用两日连续跌停价格与最近250个完成交易日两日最低收益1%分位对应价格中的更差者。历史不足250日时只使用连续跌停情景。三者均为压力估计，不是最大损失保证。

### 3.3 不制造长期空仓

- 每次输出上限仍为1只正式候选、2只条件观察和5只深度观察；这些都是上限，不是必须填满的配额。
- 当没有任何股票满足对应状态时，必须输出 `position=empty / reason=no_qualified_candidate`，并明确“当前无符合条件的候选，保持空仓”。空仓是合法生产状态，不得为降低连续空仓天数制造虚假观察项。
- 有条件观察但没有正式候选时，条件观察必须给出“下一次验证时间、确认触发、失效条件”，并延续到当日下午或次日上午验证，不能直接消失。
- 条件观察使用“观察区/确认触发”措辞，不显示为可直接挂单的“买入区”。
- 只有进入 `entry_active` 后才发布带有效期的执行价格；有效期结束或证据过期立即取消。
- 正常通过分钟确认、行业证据和经济性要求的股票仍可交易，不新增全局性一票否决。

### 3.4 市场与行业环境过滤

市场级硬否决执行本文 `INTRADAY_MARKET_VETO_V2`。指数集合固定为沪深300、中证1000、创业板指和科创50，所需实时数据在决策时均不得超过90秒。

```text
index_open_reference(i) =
    exchange official open
    or first valid quote at/after 09:30 if official open missing

index_1300_reference(i) =
    first valid quote at/after 13:00 and no later than 13:00:30

intraday_vwap(i) =
    sum(index_typical_price(m) * index_volume(m)
        from 09:30 through latest completed minute) /
    sum(index_volume(m)
        from 09:30 through latest completed minute)

index_typical_price(m) =
    (index_high(m) + index_low(m) + index_close(m)) / 3

return_since_open(i) =
    latest_close(i) / index_open_reference(i) - 1

return_since_1300(i) =
    null before 13:00
    else latest_close(i) / index_1300_reference(i) - 1

three_minute_decline(i) =
    close_m3(i) < close_m2(i) < close_m1(i)

morning_persistent_weakness(i) =
    return_since_open(i) <= -0.8%
    and latest_close(i) < intraday_vwap(i)

afternoon_acute_weakness(i) =
    decision_at >= 13:03
    and return_since_1300(i) <= -0.8%
    and latest_close(i) < intraday_vwap(i)
    and three_minute_decline(i)

index_deteriorating(i) =
    morning_persistent_weakness(i)
    or afternoon_acute_weakness(i)

broad_market_veto =
    count(index_deteriorating among four indices) >= 3
    and intraday_advance_ratio <= 35%

growth_market_veto =
    index_deteriorating(创业板指)
    and index_deteriorating(科创50)
```

这里的 `close_m1/close_m2/close_m3` 是截至决策时该指数最近3根唯一、连续、已完成的1分钟收盘，`m1`最早、`m3`最晚；不能跨午休、缺分钟或Provider切换。指数VWAP使用同一Provider标准化后的分钟指数成交量加权指数典型价；成交量缺失、累计分母为0或单位冲突时为数据不足，不得改用简单均价。上午崩盘即使在13:00后横盘，只要开盘以来跌幅仍不高于-0.8%、指数仍位于当日VWAP下方，就继续满足 `morning_persistent_weakness`，不会因 `return_since_1300` 尚不存在或下午未继续下跌而失效。

`INTRADAY_ADVANCE_UNIVERSE_V1` 固定为09:25时上海、深圳交易所普通人民币A股，不含北交所，排除ST、`*ST`、退市整理、停牌、零成交、无有效昨收或交易所参考价、无涨跌幅限制异常日和来源状态冲突证券：

```text
advancer =
    latest_valid_price > exchange_reference_price

decliner =
    latest_valid_price < exchange_reference_price

intraday_advance_ratio =
    advancers / (advancers + decliners)
```

平盘股不进入分子或分母。实时有效报价覆盖率必须不低于冻结宇宙的95%，且 `advancers + decliners > 0`；否则为 `intraday_advance_ratio_data_insufficient`。

- `broad_market_veto=true` 时，所有新买入停止晋级。
- `growth_sensitive=true` 且 `growth_market_veto=true` 时，该股票停止晋级；这正是中科曙光回归用例的硬条件。
- 任一指数、宽度或时间戳缺失时为 `intraday_market_veto_data_insufficient`，不能盘后补算后恢复当日资格。
- 不采用“上涨家数少于1000”或“行业跌幅超过2%”作为新的全局一票否决，因为单一绝对阈值会在不同股票数量和市场波动环境下制造过度空仓。

行业证据按通道处理：

- `sector_reversal_challenger` 必须满足本文附录A.2完整定义的 `SECTOR_FIRST_REVERSAL_V1`；
- 趋势延续、强势回踩和高动量通道中，行业同步失败把机会质量从 `high` 降为 `medium`，不会单独形成全市场硬否决；
- 行业有效成分少于20只或覆盖率低于80%时，行业证据为 `data_insufficient`，不得由语言模型推断。

行业证据在不同阶段变为 `data_insufficient` 时，降级路径固定如下：

- `unassessed/deep_watch/conditional_watch → data_insufficient`：清除当日行业确认计数和晋级资格，保留已冻结的主通道与缺失原因，不得继续显示条件触发为有效。
- `confirmed_candidate → data_insufficient`：撤销候选名额和未发布计划；数据恢复后只能回到 `deep_watch`，重新走完行业、市场与3分钟确认。
- `plan_published/entry_active → canceled / industry_data_insufficient`：已发布买区立即失效，当日不得从 `canceled` 恢复。
- `data_insufficient → deep_watch` 只允许在计划截止前，由同一已登记Provider或合规降级源恢复完整、新鲜数据后发生；恢复不继承旧连续分钟。到达截止时间仍未恢复则进入 `rejected_for_day` 或已发布计划的 `expired`。

## 4. 入场状态机

状态图必须覆盖正常、异常和终止分支：

```text
unassessed
  ├─→ data_insufficient ─→ deep_watch / rejected
  ├─→ rejected ─────────→ deep_watch
  └─→ deep_watch
        ├─→ data_insufficient / rejected
        └─→ conditional_watch
              ├─→ data_insufficient / canceled
              └─→ confirmed_candidate
                    ├─→ data_insufficient / canceled
                    └─→ plan_published
                          ├─→ canceled
                          └─→ entry_active
                                ├─→ canceled / expired
                                ├─→ user_reported_not_executed
                                └─→ user_reported_executed

canceled / expired
  └─→ user_reported_not_executed / execution_unknown
```

转换证据固定如下：

| 转换 | 必要条件 |
|---|---|
| `unassessed → deep_watch` | 价格、日线、行业映射和可成交状态数据完整；至少一个通道背景初筛通过；尚未完成执行确认 |
| `unassessed → data_insufficient` | 任一主通道必要字段缺失、来源冲突或超时 |
| `unassessed → rejected` | 所有通道背景硬条件均失败，或证券不可参与 |
| `deep_watch → conditional_watch` | 主通道冻结；结构支撑、观察触发和失效线可构造；机会质量至少为 `medium`；尾部风险不是 `high` |
| `deep_watch/conditional_watch → data_insufficient` | 行业、市场、分钟或主通道必要数据在晋级前缺失、冲突或超时；清除当前确认计数 |
| `conditional_watch → confirmed_candidate` | 最近3根已完成分钟唯一、连续且新鲜；收盘保持触发基准；VWAP、活动度、行业/事件证据、市场否决、禁追价和正常可成交性全部通过 |
| `confirmed_candidate → data_insufficient` | 计划发布前行业、市场或执行必要数据失效；释放正式候选名额 |
| `confirmed_candidate → canceled` | 候选在计划发布前明确违反市场、经济性、风险或截止条件，且不是数据缺失 |
| `data_insufficient → deep_watch` | 截止时间前数据恢复完整、新鲜且来源合规；全部时序证据从零重算 |
| `data_insufficient → rejected` | 到达该配置当日确认截止时间仍未恢复，或恢复后主通道硬条件失败 |
| `rejected → deep_watch` | 仅当拒绝原因属于当日可逆市场条件且尚未到截止时间；结构性、证券资格和经济性拒绝当日不可恢复 |
| `confirmed_candidate → plan_published` | 买区、禁追价、保护位、第一/第二卖区和手续费后经济性在买区上沿通过；账户/每100股风险通过；占用当天正式名额；不可变计划与证据已落盘 |
| `plan_published → entry_active` | 发布后使用不超过90秒数据完成首次复核；价格仍在买区、不高于禁追价，最近3根确认分钟仍有效；证券仍正常可成交；账户风险与禁止对冲式加仓闸门均通过 |
| `plan_published/entry_active → canceled` | 触发基准、VWAP、活动度、行业证据、市场环境、买区、禁追价、数据质量、账户风险、禁止对冲式加仓闸门或可成交性任一失效 |
| `entry_active → expired` | 到达冻结的计划到期时间仍无已确认成交 |
| `entry_active → user_reported_executed` | 收到用户明确成交报告；先记为待对账执行记录，不直接视为券商已校验 |
| `entry_active → user_reported_not_executed` | 用户明确报告未下单或未成交，且无相反券商证据 |
| `canceled/expired → user_reported_not_executed` | 用户确认取消或到期前未成交 |
| `canceled/expired → execution_unknown` | 无法确认取消或到期前是否已成交 |

- `conditional_watch`：只能描述观察触发和失效条件，禁止发布“可以买”“挂单价”。
- `confirmed_candidate`：已完成连续分钟、VWAP、量能、行业和市场环境验证，但尚未发布执行计划。
- `plan_published`：保存买区、禁追价、保护位、目标、有效起止时间、证据路径和模型版本。
- `entry_active`：最近三根完成分钟仍然连续、新鲜且证据未失效，才允许用户执行。
- 对话层、自动化和Skill均不得跳过状态；非法转换返回 `illegal_state_transition`。

`CANDIDATE_SLOT_V1` 定义每天1个正式候选名额的释放和重分配：

1. 名额按 `strategy_profile_id + trade_date` 分账；`confirmed_candidate` 首次晋级时预留，`plan_published` 时正式占用。
2. `confirmed_candidate → data_insufficient/canceled` 且尚未发布计划时立即释放。截止时间前，可以分配给仍处于 `confirmed_candidate` 的下一只股票。
3. 下一只股票按“机会质量高优先、尾部风险低优先、手续费后收益风险比高优先、股票代码升序”确定唯一顺序；不得临时改用语言模型总分。
4. `plan_published/entry_active → canceled` 后，只有 `user_execution=user_reported_not_executed` 或可验证为未成交，且仍早于配置截止时间，才释放名额。`execution_unknown` 不释放，防止同一配置实际持有两笔却只记录一笔。
5. 释放后必须生成新的 `decision_id` 和完整新鲜证据，不能把上一只股票的分钟确认、发布时间或模型解释转移给替补。
6. 到达截止时间后不再重分配；当日输出明确 `formal_slot=empty_after_cutoff`。

### 4.1 分钟数据异常处理

连续分钟确认的数据质量合同固定如下：

1. **无成交分钟与交易会话边界**：只有连续竞价时段 `[09:30, 11:30)`、`[13:00, 14:57)` 内，证券状态正常但成交量为0的完整分钟才记为 `zero_trade_minute`；该分钟可保存为零活动记录，但不能计入确认窗口，当前连续确认计数清零。09:15至09:30开盘集合竞价、11:30至13:00午休、14:57至15:00收盘集合竞价和闭市后均不生成 `zero_trade_minute`，不计入也不延长确认窗口。午休和集合竞价是会话边界，不标记数据异常，但禁止把边界两侧分钟拼成连续确认窗口；13:00后必须从零重新累计。
2. **延迟**：最新数据年龄超过90秒时，`plan_published` 不得进入 `entry_active`，已有 `entry_active` 转为 `canceled / data_stale`。连续延迟超过180秒时允许切换已登记备用源，但切换后必须重新累计3根完成分钟。
3. **重复或缺分钟**：时间戳重复、非连续或倒序时返回 `entry_minutes_duplicated`、`entry_minutes_not_consecutive`，不得插值补齐。
4. **价格异常**：超出交易所合法涨跌幅、OHLC关系错误、量额隐含均价不在分钟高低区间，或跨源价格差超过 `max(3*tick_size, 0.3%)` 时标记 `data_conflict`。
5. **瞬时尖峰**：单点尖峰只有在同源成交量额与下一根完成分钟可复核时才保留；否则不能触发买区、卖区或保护位。
6. **来源切换**：不同Provider的分钟不得拼成同一个3分钟确认窗口。

数据异常只使当前股票或当前阶段进入 `data_insufficient/canceled`；除非市场级数据整体失效，不得把单股数据异常扩展为全市场空仓。

## 5. 持仓与次日退出状态机

新增持仓生命周期：

```text
opened_t1_locked
  → next_open_observe
  → intraday_position_monitor
  ├─ failed_open_exit
  ├─ profit_protection
  ├─ structural_stop_exit
  ├─ target_exit
  ├─ time_exit
  └─ exit_unavailable

failed_open_exit / profit_protection /
structural_stop_exit / target_exit / time_exit
  ├─→ closed
  ├─→ exit_unavailable
  ├─→ user_deviation_hold
  └─→ execution_unknown

exit_unavailable
  → forced_carry_unavailable
  → subsequent_session_exit_watch
  ├─→ closed
  ├─→ exit_unavailable
  ├─→ user_deviation_hold
  └─→ execution_unknown
```

### 5.1 T+1锁定期

- 买入后立即保存次日四套计划：正常开盘、强势高开、弱势低开、跳空越过保护位。
- 当日不可卖不代表风险不存在；盘后必须计算次日跌停压力和账户总压力。
- 禁止因另一持仓亏损而进行对冲式加仓。

禁止对冲式加仓必须在 `plan_published → entry_active` 前执行 `LOSS_OFFSET_ADD_GATE_V1`，不能只在事后审计：

1. 候选与账户内亏损持仓为同一证券时，禁止新增，原因码 `underwater_same_symbol_add_blocked`。
2. 亏损持仓已经失守保护位、处于 `exit_unavailable/user_deviation_hold/execution_unknown`，或存在未完成退出意图时，与其同一主行业或冻结 `correlation_group_id` 的候选禁止新增，原因码 `unresolved_loss_correlated_add_blocked`。
3. 不采用“账户内任一持仓浮亏即禁止所有同向或相关买入”的全局规则。尚未失守保护位、执行状态明确的轻微浮亏持仓，仍按 `ACCOUNT_RISK_V1` 的总压力、行业压力和相关市值上限判断，避免把事前拦截扩大成长期空仓。
4. 闸门输入必须来自同一 `ACCOUNT_SNAPSHOT_V1`；账户输入不完整时不得宣称闸门通过，只能保持 `plan_only`。

### 5.2 次日开盘验证

附录A.6已经定义 `opening_reference_price`、`open_proxy_price`、`first_normal_executable_window` 和 `open_validation_end`。开盘验证从第一正常可成交窗口开始，而不是固定等到10:00：

1. 开盘达到第二卖区：第一正常可成交窗口全部退出。
2. 开盘达到第一卖区：一手仓位全部退出；可拆分仓位先兑现一部分。
3. 高开或冲高后满足附录A.7的 `failed_open_structure` 或 `early_target_reversal`：进入 `failed_open_exit`。
4. 开盘直接越过保护位：第一正常可成交窗口进入 `structural_stop_exit`。
5. 无法成交：记录 `exit_unavailable`，不得把理论止损价冒充实际成交。

开盘卖区按附录A.7 `EXIT_EXECUTION_V1` 的互斥区间判定：先检查第二卖区，再检查第一卖区。`open_proxy_price` 不低于第二卖区下沿时只命中规则1并全部退出，不再同时执行规则2；仅当它不低于第一卖区下沿且低于第二卖区下沿时才执行规则2。第二卖区不可构造时跳过规则1。

达到附录A.6定义的 `open_validation_end` 仍有仓位时，立即进入 `intraday_position_monitor`，持续到14:30：

- 任一正常交易分钟触及第一或第二卖区，按数量合同执行；
- `open_validation_end` 前发生 `early_target_reversal` 时统一进入 `failed_open_exit`，不等待18分钟活动度基线；
- 13:00后突然跳水并在完成分钟跌破保护位，立即进入 `structural_stop_exit`，不等下一固定复盘节点；
- 触及跌停但仍存在正常成交窗口时提交退出意图；一字跌停或无正常窗口进入 `exit_unavailable`；
- 午后连续走弱、没有触及卖区也没有触及保护位时，14:30进入 `time_exit`；
- 所有触发均执行附录A.6完整定义的 `EXECUTABILITY_V1`，不能把行情触价记录成用户实际成交。

### 5.3 接近目标后的利润保护

利润保护只管理 `open_validation_end` 之后尚未退出的仓位；开盘阶段的相同弱势证据由 `failed_open_exit` 管理，二者按附录A.7 `EXIT_EVENT_PRIORITY_V1` 互斥。

- 达到待校准的目标推进比例后，若价格出现冲高回落、跌破开盘参考价/VWAP或量价失速，进入利润保护。
- 该机制用于处理杰瑞股份“最高142.00、距离142.45仅0.45元”这类路径。
- 参数搜索空间固定为：

```text
target_progress_activation ∈ {0.60, 0.70, 0.80, 0.90}
drawdown_from_local_high_atr ∈ {0.30, 0.50, 0.75, 1.00} × ATR14
weakness_confirm_minutes ∈ {1, 2, 3} completed minutes
profit_activity_ratio_max ∈ {0.50, 0.65, 0.80, 0.90}
```

激活价、跳空到达与日内冲高到达按附录A.7冻结。运行时进度只使用实时可执行价格，不用事后日高回写：

```text
current_target_progress =
    (latest_executable_price - entry_price) /
    (first_zone_lower - entry_price)

max_target_progress_since_activation =
    (runup_high_since_activation - entry_price) /
    (first_zone_lower - entry_price)
```

`first_zone_lower <= buy_zone_upper` 的新计划已经由附录A.4拒绝。遗留持仓若 `first_zone_lower <= entry_price`、入场价未对账或第一卖区不可构造，则 `target_progress=null / target_progress_data_insufficient`，利润保护不得使用替代分母。此时处理固定为：

- 本笔交易完全跳过 `profit_protection` 分支，不等待人工临场创造替代公式，继续执行第一/第二卖区、`failed_open_exit`、结构保护位和14:30时间退出等基础合同。
- 新入场计划若在发布前已出现 `first_zone_lower <= entry_price`，同时属于手续费后经济性失败，不得进入 `plan_published`；该不可用路径主要用于遗留持仓、对账冲突或卖区重建失败的运行时保护。
- 审计保存 `profit_protection_applicability=not_applicable`、精确原因码和缺失字段，单独统计 `target_progress_unavailable_rate`；不得把它计入“利润保护未触发”样本，也不得静默删除整笔交易。

`activation_at` 是附录A.7定义的首次可执行越过时刻。“确认后的局部高点”固定为 `runup_high_since_activation`，从 `activation_at` 开始累计并包含触发分钟内可验证的最高可执行价；不得把激活前已经形成的顶部替换为激活时价格，也不得在回落后追认激活。

在 `open_validation_end` 之后，已经存在有效 `activation_at`，且以下至少两项持续满足 `weakness_confirm_minutes` 时触发 `profit_protection`：

1. 从 `runup_high_since_activation` 回撤达到 `drawdown_from_local_high_atr × ATR14`；
2. 完成分钟收盘跌破 `opening_reference_price`；
3. 完成分钟收盘跌破次日实时VWAP；
4. `profit_activity_ratio <= profit_activity_ratio_max`，且价格不再创新高。

其中 `profit_activity_ratio` 沿用成交额口径：

```text
latest_3m_amount = 最近3根唯一、连续、已完成分钟的成交额之和
baseline_3m_amount = 此前15根已完成分钟内所有连续3分钟成交额窗口之和的中位数
profit_activity_ratio = latest_3m_amount / baseline_3m_amount
```

活动度项至少需要此前15根基线分钟和随后3根观察分钟；`baseline_3m_amount<=0`、成交额缺失、分钟跨午休/Provider或分钟不连续时，该项为 `activity_evidence_insufficient`，不能按真计数。它只是四项弱势证据之一，不是利润保护总开关：开盘阶段可由 `early_target_reversal` 在第2个完成分钟后触发 `failed_open_exit`；开盘验证后也可由任意两项有效价格弱势证据触发，无需等待18分钟。

价格类弱势条件必须持续 `weakness_confirm_minutes` 根唯一、连续、已完成分钟；活动度条件在同一确认窗口末端计算。若确认期间重新创新高、重新站回开盘价和VWAP，当前弱势确认计数清零。

校准使用按交易日顺序的走步验证，计入佣金、印花税、滑点、一手整体退出和不可成交路径。目标函数不是最高胜率，而是在“正式候选率不显著下降、过早退出率受控”的约束下，提高手续费后中位收益并降低95%尾部损失。

- `early_target_reversal → failed_open_exit` 是确定性结构规则，完成回归测试后即可生产，不等待利润保护网格校准。
- 跨通道共享的四个利润保护参数使用合并样本校准：至少60个可执行样本、覆盖至少3个通道、每个纳入通道不少于10个样本，并覆盖至少20个交易日，才可选择一套共享生产参数。
- 少于20个总样本只影子记录；20至59个样本只做流程和方向检查。
- 只有通道专属覆盖达到50个可执行样本并覆盖至少10个交易日，才允许建立该通道覆盖共享参数的专属版本；否则继续使用共享参数。
- 参数、训练窗口、验证窗口、通道构成和回滚条件写入独立模型版本，禁止在Skill中写死未版本化常量。

### 5.4 第一卖区与第二卖区

两级卖区执行本文 `SELL_ZONES_V1`：

```text
first_center = min(valid resistance_1,
                   buy_zone_upper + 0.75 × ATR14)
first_zone = [first_center - 0.05 × ATR14,
              first_center + 0.05 × ATR14]

second_center = min(valid resistance_2,
                    buy_zone_upper + 1.25 × ATR14)
second_zone = [second_center - 0.05 × ATR14,
               second_center + 0.05 × ATR14]
```

- `resistance_1/2` 在决策时从有效20日摆动高点与截至确认时的当日高点中冻结；没有有效阻力时只使用ATR项。
- `first_zone_lower` 必须严格高于 `buy_zone_upper`；否则计划构造失败并返回 `sell_zone_inside_buy_zone`。
- 第二卖区下沿必须高于第一卖区上沿，否则 `second_zone=not_available`。
- 第一卖区是基础兑现区；第二卖区只用于可拆分仓位且 `extension_allowed=true` 的强势延伸。
- 只有100股时，第一卖区首次正常可执行触发即全部退出，不等待第二卖区。

### 5.5 T+2及以后

本策略仍是隔夜纯短线，正常情况下次日14:30必须产生退出意图，不允许主动把失败交易改称波段：

- 因停牌、一字跌停或市场异常无法退出：进入 `forced_carry_unavailable`，每个后续交易日从第一正常可成交窗口开始执行 `subsequent_session_exit_watch`，直到退出；风险线不向下放宽。
- 市场可成交但用户明确选择不退出：进入 `user_deviation_hold`。策略继续提供风险管理，但该持仓不再使用原目标，不计入策略按计划收益，审计记为用户执行偏离。
- 用户没有反馈：执行状态保持 `execution_unknown`；理论退出路径与用户实际持仓分账。
- 到T+5仍未退出不产生新的“长期持有”状态；只允许 `forced_carry_unavailable`、`user_deviation_hold` 或 `execution_unknown`。
- T+5不是停止管理的期限。只要权威持仓台账仍显示数量大于0，每个后续交易日都必须更新当前价、可售数量、最近正常可成交窗口、未完成退出意图、保护位状态和下一退出动作，直至 `closed` 或对账证明持仓不存在。
- T+6及以后继续沿用 `subsequent_session_exit_watch`，不得静默移出持仓列表、停止刷新或恢复原盈利目标。

### 5.6 一手仓位

- 只有100股时禁止反T，因为无法保留底仓。
- 第一卖区、强势失败或结构止损均对应全部退出。
- 不为等待第二目标放弃已经出现的正常退出窗口。

## 6. 用户输出合同

每只股票只能出现以下一个用户可见主状态；内部状态与展示不得自行合并：

| 用户可见状态 | 内部状态 | 必须显示 | 是否可执行 |
|---|---|---|---|
| 待评估 | `unassessed` | 数据截面、尚未评估原因 | 否 |
| 数据不足 | `data_insufficient` | 缺失字段、来源、最后有效时间、下次重试或当日终止时间 | 否；不得显示旧买区 |
| 当日排除 | `rejected/rejected_for_day` | 主原因码、是否可逆、最早重新评估日 | 否；从候选排序移除 |
| 深度观察 | `deep_watch` | 背景、尚缺证据、下一检查点 | 否 |
| 条件观察 | `conditional_watch` | 确认触发、失效条件、下一检查时间 | 否 |
| 已确认候选 | `confirmed_candidate` | 通道、证据时间、尚未发布计划原因、名额状态 | 否 |
| 正式计划 | `plan_published` | 买区、禁追价、保护位、卖区、有效期、待复核项 | 待激活 |
| 入场有效 | `entry_active` | 当前可执行区、数据时间、剩余有效期、取消条件 | 是，仅在有效期内 |
| 已取消 | `canceled` | 取消时间、原因码、是否确认未成交 | 否；从可买列表移除，不再显示原买区为有效 |
| 已过期 | `expired` | 到期时间、是否确认未成交 | 否；不得恢复当日计划 |
| 执行未知 | `execution_unknown` | 理论计划、缺失的用户/券商证据、对账请求 | 否；不得假定空仓或已持仓 |
| 已报告成交待对账 | `user_reported_executed/unverified` | 报告价格、数量、时间、待核验字段 | 仅持仓风险提示 |
| T+1锁定 | `opened_t1_locked` | 数量、成本、今日不可售、次日四套退出计划 | 仅风险管理 |
| 开盘观察 | `next_open_observe` | 开盘代理价、第一正常窗口、卖区/保护位判定、验证结束时间 | 仅卖出管理 |
| 盘中持仓监控 | `intraday_position_monitor` | 当前价、可售数量、卖区、保护位、下一检查时间 | 仅卖出管理 |
| 退出已触发 | `failed_open_exit/profit_protection/structural_stop_exit/target_exit/time_exit` | 退出原因、数量、触发时间、市场可执行状态、对账状态 | 提交退出意图，不等于已成交 |
| 暂不可退出 | `exit_unavailable/forced_carry_unavailable` | 不可成交原因、首次及最近尝试、下次交易日动作 | 持续退出管理 |
| 后续交易日退出监控 | `subsequent_session_exit_watch` | 当前价、可售数量、未完成退出意图、第一正常窗口 | 持续退出管理 |
| 用户偏离持有 | `user_deviation_hold` | 原策略已失效、当前风险、用户未执行事实 | 只给风险退出，不恢复原目标 |
| 已关闭 | `closed` | 已对账成交、数量归零、实际或未知收益状态 | 否 |

不得同时出现“空仓”与某只股票的可执行买区。不得在正式状态取消后继续引用原买区。

### 6.1 成交报告与持仓对账

策略状态和成交对账状态分离：

```text
strategy_execution_state =
    user_reported_executed / user_reported_not_executed / execution_unknown

reconciliation_state =
    unverified / screenshot_verified / broker_record_verified /
    conflict_pending / corrected
```

每条用户成交报告至少保存代码、方向、数量、价格、报告时间、推定成交日、证据来源和 `execution_id`。

- 只有口头报告时先记 `unverified`，可以更新临时持仓，但审计必须标明未对账。
- 用户不报告时，当日终态为 `execution_unknown`，不得推定未成交；下一份权威持仓截图或成交记录到达时补做对账。
- 对账满足 `昨日数量 + 买入 - 卖出 - 转出 + 转入 = 当前数量`；数量、成本或可售数冲突时进入 `conflict_pending`。
- 用户后续更正价格或数量时追加 `correction_record`，不得覆盖原报告和原状态转换历史。
- 市场理论可执行、用户报告成交和券商证据三套结果始终分账；概率训练只使用冻结理论协议，用户实际收益只使用已对账执行记录。

## 7. 审计与参数优化

每笔交易拆成四类责任：

1. `selection_failure`：股票或通道选择错误。
2. `entry_failure`：确认不完整、买区不合理或追价。
3. `exit_failure`：目标刚性、开盘失败未退出或止损拖延。
4. `execution_failure`：跌停、流动性、用户未执行或成交信息缺失。

新增指标：

- 正式状态绕过次数；
- 条件观察误表达率；
- 次日开盘失败退出率；
- 接近目标后利润回吐幅度；
- 风险线首次失守至实际退出的交易分钟；
- 一手仓位尾部损失率；
- 正式候选率、条件观察转化率和连续空仓天数；
- 各通道手续费后期望收益及最大不利变动。

结构性缺陷与数值参数的边界固定为：

- **结构性变更**：改变状态、状态转换、字段含义、算法数据流、事件树、证据持久化、输出权限或版本兼容逻辑。例如把“固定目标触发”扩展为“目标推进后利润保护”，属于结构性变更，可以先实现为影子机制。
- **数值变更**：算法和事件定义不变，只调整常量、区间或权重。例如把 `target_progress_activation` 从0.80改为0.70，属于数值变更。
- **算法替换**：即使表面上只改公式，只要改变事件标签或样本含义，仍按结构性变更处理，并必须建立新合同版本和旧数据迁移规则。

结构性缺陷可以立即修复，但新结构中的数值参数在达到校准门槛前只能影子运行；VWAP、ATR、目标推进比例、回撤幅度等数值参数仍遵守至少3次同类失败和最低样本门槛。新通道晋级继续执行更高的影子样本要求。

## 8. 实施顺序、基础设施与发布合同

策略文档是规则源，`ObjectiveDataProvider` 和Schema是事实层，Skill是执行适配层。任何Skill、自动化提示词或模型提示词不得先于事实层和迁移层自行实现VWAP、ATR、买区、保护位、卖区、状态转换或审计标签。

### 8.0 阶段一：实施前基础设施

基础设施完成前，不得修改8个Skill的生产逻辑。

1. 先实现 `ObjectiveDataProvider`：
   - 按附录A的公式实现纯函数事实层，不依赖任何Skill、不调用模型、不读取自然语言结论。
   - 输入包括分钟线、日线、行业指数、市场宽度、证券状态、账户快照和交易历史；输出必须是带 `as_of`、`available_at`、Provider、公式版本、证据ID和原始响应哈希的冻结结构化快照。
   - VWAP、ATR、活动度、买区、禁追价、保护位、卖区、开盘代理价、正常可成交窗口、市场否决和行业同步，只能由Provider或其下游确定性模块计算。
   - 对杰瑞股份、中科曙光的历史分钟数据先跑回放，验证 `early_target_reversal`、`morning_persistent_weakness`、保护位、卖区和退出状态与本文档预期一致。

2. 先建立Schema和迁移脚本：
   - 同时定义旧记录读取Schema和新版写入Schema；旧记录读取时补 `strategy_version=legacy`、`schema_version=legacy`、缺失字段原因码和迁移时间，不得覆盖原始记录。
   - 新版记录写入时强制校验 `strategy_profile_id`、`strategy_version`、`lifecycle_contract_version`、`schema_version`、`execution_rule_version`、`skill_version` 六个版本字段。
   - 迁移脚本必须在杰瑞股份和中科曙光的历史记录上回放，确认成交价、数量、可售状态、推荐时间、退出意图和审计标签不丢失。
   - JSON Schema与SQLite表结构任选其一作为生产存储主约束，但两者若同时存在，字段含义必须由同一Schema源生成，禁止手写两份不一致结构。

3. 先建立测试框架：
   - 使用 `pytest` 参数化承载§9的全部失败、正向和新机制功能用例；第一批先提交空壳与固定夹具，未实现模块标记为 `xfail(strict=True)` 或等价的“预期失败”。
   - 每实现一个模块，先让对应测试从失败变为通过，再进入下一模块；不得在最后一次性补测试。
   - 固定夹具至少包含杰瑞股份分钟回放、中科曙光买入日市场与行业回放、9种双轴矩阵边界样本、双配置隔离样本、旧记录迁移样本。

### 8.1 实际实施顺序

本文档§8的生产实施顺序固定调整为：

| 顺序 | 内容 | 依赖 | 验收门槛 |
|---|---|---|---|
| 1 | Schema、迁移脚本、`ObjectiveDataProvider` | 无 | 杰瑞/中科历史记录可迁移；Provider快照可复现 |
| 2 | 测试框架与§9用例空壳 | 1 | 参数化测试可运行，未实现项明确预期失败 |
| 3 | §4入场状态机与§5退出状态机核心 | 1、2 | 合法边通过，非法边失败，状态输出映射完整 |
| 4 | 附录A公式实现 | 1、2 | VWAP、ATR、买区、保护位、卖区、活动度、开盘结构单元测试通过 |
| 5 | 双轴判断、市场否决、行业证据 | 3、4 | 9种矩阵组合与上午/下午市场否决回放通过 |
| 6 | 审计、对账与用户报告校验 | 3 | 未报告、错报、截图冲突和更正路径唯一 |
| 7 | 版本兼容、原子切换与回退指针 | 1、3、6 | 同一 `decision_id` 全链路版本一致，失败不生成 `effective_from` |
| 8 | 逐个Skill适配Provider冻结快照 | 1至7 | 每改一个Skill跑对应测试；未改Skill继续旧版生产或只读适配 |
| 9 | 自动化提示词、模型版本文件和运行手册更新 | 8 | 自动化不得越过Provider，不得生成未版本化结论 |

优先级不得改成“先改Skill再补Provider”。若为临时排障必须动Skill，只允许增加只读兼容层或日志，不得改变买卖决策含义。

### 8.2 版本兼容与原子切换

所有决策、计划、持仓状态和审计记录新增：

```text
strategy_profile_id
strategy_version
lifecycle_contract_version
schema_version
execution_rule_version
skill_version
```

过渡期采用“先兼容、后激活”：

1. 先发布能够同时读取旧版和新版记录的Schema与审计器；
2. 再发布支持新版但默认关闭的代码和Skills；
3. 使用固定历史样本和当日影子数据验证新合同；
4. 在交易日09:25前冻结 `effective_from`，一次性切换生产写入版本；
5. 同一 `decision_id` 从候选到审计必须使用同一 `strategy_version`，禁止买入按旧版、卖出或审计按新版。

旧记录缺少新字段时仅在旧版本读取路径允许为空；新版记录缺少任一必填字段必须 `schema_rejected`。过渡期间不得出现“策略合同已生效但Skill仍按旧规则发布买点”的混合状态。

步骤3验证失败时执行 `VERSION_ACTIVATION_GATE_V1`：

- 任一结构测试、双实现一致性、固定回归样本、Schema迁移或影子验收指标失败，均不得设置 `effective_from`；旧版继续作为唯一生产写入版本，新版保持关闭或影子状态。
- 正式候选率显著下降、连续空仓天数上升而尾部损失未改善，或利润保护过早退出率越过预设回滚线，视为验收失败；不得用临时放宽其他通道抵消。
- 失败项修复后生成新的候选版本号，重新完成步骤1至3；不得覆盖失败版本的影子记录。
- 生命周期合同、Schema、执行引擎、Skills和自动化提示词属于一个原子发布单元，不允许只切换其中一部分。只有明确声明为向后兼容、且不改变决策含义的读取适配器可以单独先行。
- 若切换后才发现严重缺陷，停止生成新版决策，并在下一交易会话前把生产写入指针整体回退到最近通过验收的版本；已经产生的 `decision_id` 仍按其冻结版本完成退出和审计，不得中途改版。

预计涉及：

- `a-share-t1-rank-sandbox`：候选覆盖、双轴判断、正式状态输出。
- `a-share-quant-sandbox`：盘中确认、目标推进、冲高失败证据。
- `a-share-technical-lab`：趋势、分布和高波动背景标签。
- `a-share-prediction-audit`：四类失败归因和参数优化门槛。
- `a-share-portfolio-optimizer`：持仓生命周期、相关压力和禁止对冲式加仓。
- `a-share-watchlist-shortline`：固定自选池的入场与次日退出状态。
- `a-share-watchlist-shortline-audit`：次日开盘至14:30的实际退出审计。
- 全市场T+1策略文档、自选池生产规范、模型版本和相关自动化提示词。

全市场策略和固定自选池策略继续保持数据域、版本和候选来源隔离；共享的是经过版本化的生命周期合同，不共享临时评分或当日候选。

### 8.3 高风险模块独立验证

以下模块必须在集成前单独验证；未通过时不得进入Skill适配阶段：

1. `early_target_reversal`：
   - 使用杰瑞股份历史分钟数据回放，覆盖“开盘高于成本、5至10分钟冲近第一卖区、随后跌破开盘参考价与VWAP”的路径。
   - 验证其在开盘验证期内进入 `failed_open_exit`，不被18分钟活动度基线阻塞，也不与 `profit_protection` 竞争状态。

2. `morning_persistent_weakness`：
   - 使用中科曙光买入日的指数、行业和全市场上涨家数数据回放。
   - 验证09:30至11:30的市场崩盘可以触发 `broad_market_veto` 或 `growth_market_veto`，不依赖13:00后的 `return_since_1300`。

3. 双轴矩阵9种组合：
   - 使用边界样本逐一验证机会质量 `high/medium/low` 与尾部风险 `low/medium/high` 的唯一输出。
   - 特别验证“高机会+高风险”不得正式晋级，“中机会+低风险”不得越级，“高机会+中风险”必须经过 `ACCOUNT_RISK_V1` 的100股档位校验。

4. 双配置隔离：
   - `CANDIDATE_SLOT_V1`、状态账本、样本分母和审计聚合必须以 `strategy_profile_id` 作为第一层Key。
   - 同一股票同一时点同时进入 `watchlist_t1_v1` 与 `full_market_t1_v1` 时，只能共享Provider事实快照，不能共享候选名额、状态或命中统计。

### 8.4 影子运行与切换窗口

影子运行最低要求固定为5个完整交易日。影子期内旧版继续作为生产输出，新版只产生带版本号的影子快照和审计，不得影响实际建议。

影子运行必须比较：

- 正式候选差异：新系统是否过度拒绝，是否制造虚假候选；
- 退出触发差异：`failed_open_exit`、`profit_protection`、`structural_stop_exit`、`time_exit` 的触发时间、价格和可执行状态；
- 质量指标：手续费后收益、最大单笔尾部损失、连续空仓天数、条件观察转化率；
- 数据完整性：Provider延迟、缺分钟、Provider切换、市场宽度覆盖率、行业数据覆盖率；
- 配置隔离：两个 `strategy_profile_id` 的候选名额、账本和审计分母不得串用。

影子运行第1个交易日若出现全天零候选、关键状态机卡死、迁移丢字段、数据源严重延迟或退出意图无法生成，可立即判定失败并回到修复阶段，不必等待满5日。

生产切换窗口：

- 优先选择周四或周五切换，给周末留出完整复盘窗口。
- 切换前一个交易日收盘后冻结所有未完成 `decision_id` 的 `strategy_version`。
- 切换日09:25前冻结 `effective_from`；09:25后不得临场改参数。
- 切换日开盘前30分钟必须人工检查Provider快照、状态机转换、候选名额、退出意图和审计写入。

### 8.5 日志、参数与分批适配

前5个交易日保留完整DEBUG日志。所有状态转换、公式计算中间值、数据不足原因、Provider响应哈希、版本字段、用户报告对账和Skill输出适配，必须可以从日志追溯到同一 `decision_id`。

`profit_protection` 在达到§5.3校准门槛前不得留空。影子运行默认固定参数为：

```text
target_progress_activation = 0.80
drawdown_from_local_high_atr = 0.50
weakness_confirm_minutes = 2
profit_activity_ratio_max = 0.65
```

这些参数仅用于影子运行和结构验证；进入生产前必须满足§5.3的样本门槛，或被明确标记为未经校准的影子提示。代码和Skill中禁止出现 `TODO: calibrate later` 但仍参与生产判断的空参数。

Skill适配分四批：

1. 第一批：Provider + Schema + 迁移脚本；不改变任何策略行为。
2. 第二批：状态机 + 附录A公式；旧Skill仍按旧版生产，新规则只影子。
3. 第三批：逐个Skill改为读取Provider冻结快照；每改一个跑对应测试，确认不影响其他Skill。
4. 第四批：模型版本、自动化提示词和 `effective_from` 一次性切换；未通过原子验收不得部分生效。

## 9. 测试与验收

### 9.1 必须新增的失败用例

1. 杰瑞股份回归：缺少持久化连续分钟证据时，条件候选不得进入 `entry_active`。
2. 杰瑞股份退出回归：次日高开并接近第一目标后失去开盘结构，应在大幅下跌前进入失败退出状态。
3. 中科曙光入口回归：成长风险恶化和买区上沿经济性失败时不得正式入场。
4. 中科曙光持仓回归：保护位失守后不得继续输出原推荐目标。
5. 对话旁路回归：自然语言请求不能把观察区转换成挂单价。
6. 一手回归：100股不得输出反T卖出后回补方案。

### 9.2 防止策略过严的正向用例

1. 趋势延续股满足完整分钟确认后仍可成为正式候选。
2. 强势回踩股完成VWAP与枢轴收复后仍可晋级。
3. 高动量股不因涨幅高被研究池统一删除；在 `HIGH_MOMENTUM_V1` 验收前只能保持影子观察，验收后再按其完整合同决定是否可正式晋级。
4. 没有当日下午正式机会时，条件候选可延续至次日上午确认。
5. 回放中正式候选率、手续费后收益和最大回撤必须同时报告，不能只看减少亏损。

### 9.3 新机制功能测试

1. 双轴矩阵的全部组合必须得到唯一结果；高机会/高风险不得正式买入，中机会/低风险不得越级为正式候选。
2. `target_progress` 在阈值下不触发、达到阈值但弱势证据不足不触发、达到阈值且至少两项弱势证据成立时触发。
3. 利润保护参数网格必须按训练/验证时间顺序运行，未来数据不得进入训练。
4. 第一、第二卖区按 `SELL_ZONES_V1` 得到唯一值；区间重叠时第二卖区必须为 `not_available`。
5. 每条合法状态边可通过，每条未列出的跳转返回 `illegal_state_transition`。
6. 次日开盘后13:00突然跌破保护位应进入 `structural_stop_exit`；一字跌停无正常窗口应进入 `exit_unavailable`。
7. T+2可成交时必须继续退出；用户拒绝退出进入 `user_deviation_hold`，不得恢复原目标。
8. 用户未报告、报告错误、截图数量冲突和后续更正分别进入唯一对账状态，历史不可覆盖。
9. 无成交分钟、90秒延迟、缺分钟、重复分钟、跨源尖峰和Provider切换均不能伪造3分钟确认。
10. `broad_market_veto` 和 `growth_market_veto` 的正反边界均需测试；单一行业偏弱不得误触发全市场空仓。
11. 新旧版本记录可分别完成全链路；同一决策混用版本必须失败。
12. 开盘代理价越过第二卖区时只能命中第二卖区优先分支并全部退出；第一卖区分支不得重复执行。
13. `target_progress` 分母不可用时利润保护标记 `not_applicable`，基础退出合同继续有效，审计不得记为普通未触发。
14. 四维利润保护参数网格必须包含 `profit_activity_ratio_max`；活动度基线不足时该证据不得按真计数。
15. `ACCOUNT_RISK_V1` 在100股档位任一上限失败时不得从高机会/中风险晋级；账户快照不完整时保持 `plan_only`。
16. 行业数据在 `conditional_watch`、`confirmed_candidate` 和 `entry_active` 分别失效时，必须得到规定的 `data_insufficient` 或 `canceled` 路径，且恢复后不得继承旧分钟证据。
17. 同标的亏损加仓及未解决亏损持仓的相关加仓必须在入场激活前被拦截；轻微浮亏但风险合同合格的非同标的机会不得被全局误杀。
18. 新版影子验证失败时不得生成 `effective_from`；原子发布单元不得部分切换，既有决策不得在回退时中途换版。
19. 午休、开收盘集合竞价不得生成异常无成交分钟；确认窗口不得跨午休或集合竞价拼接。
20. `watchlist_t1_v1` 与 `full_market_t1_v1` 对同一股票同一时点独立运行时，候选名额、状态、模型样本和审计分母不得串用；任何缺失或跨用配置ID的记录必须失败。
21. `COLLECTION_CADENCE_V1.status=deferred_capacity_review` 时，不得把全市场每分钟轮询当成生产前提，也不得因此放宽决策字段的新鲜度和数据完整性要求。
22. 09:30至11:30三个以上指数从开盘下跌至少0.8%、位于VWAP下方且全市场上涨占比不高于35%时，必须触发上午市场否决；不得等待 `return_since_1300`。
23. `m1/m2/m3`、上涨家数宇宙、行业同步、活动度、禁追价、买区、保护位、开盘代理价和正常可成交窗口的任一必要字段缺失时，必须得到唯一数据不足状态，不得由自然语言补值。
24. 开盘后第2至第10分钟到达目标激活价并连续两根跌破开盘参考价与VWAP时，必须进入 `failed_open_exit`，不得因活动度基线不足继续等待。
25. 开盘即位于激活价上方必须标为 `gap_arrival`；从下方盘中首次穿越才标为 `intraday_runup`，二者的 `activation_at` 不得由盘后最高价重写。
26. 开盘验证期内同时满足失败开盘和利润保护证据时只能进入 `failed_open_exit`；验证结束后才允许 `profit_protection`。
27. 状态图、转换表和用户输出映射必须覆盖 `data_insufficient/rejected/canceled/expired/execution_unknown/closed`，三者状态集合不一致时结构测试失败。
28. T+6及以后权威台账仍有持仓时必须继续输出当前价、可售数量、未完成退出意图和下一退出动作，不得从策略视野消失。
29. `first_zone_lower <= buy_zone_upper` 时，即使实际成交价更低也必须以 `sell_zone_inside_buy_zone` 拒绝计划。
30. `confirmed_candidate` 释放名额后只有排序下一且重新通过新鲜证据的股票可以接替；`execution_unknown` 不得释放已经发布的名额。
31. `afternoon_swing_low` 只能选择13:00至m1开始前、且已在m1前完成右侧两分钟确认的最近摆动低点；m1后形成或确认的更优低点不得回写，缺失时必须返回 `afternoon_swing_low_missing`。
32. 任一Skill若绕过 `ObjectiveDataProvider` 自行计算VWAP、ATR、保护位、卖区或状态标签，适配测试必须失败。
33. 旧记录缺少六个版本字段时只能走legacy读取迁移路径；新版写入缺任一版本字段必须 `schema_rejected`，不得静默补默认生产值。
34. `pytest` 用例空壳必须先于对应模块实现存在；未实现模块必须被标记为预期失败，禁止因跳过测试而显示通过。
35. 新版影子运行少于5个完整交易日时不得切换生产；若第1日出现全天零候选、状态机卡死或迁移丢字段，可提前失败并回到修复阶段。
36. `profit_protection` 校准门槛未满足时只能使用固定影子参数输出影子证据；空参数或TODO参数参与生产判断必须失败。
37. 前5个交易日DEBUG日志必须能按 `decision_id` 串起Provider快照、公式中间值、状态转换、Skill输出和审计记录；缺任一环节视为验收失败。

### 9.4 验收标准

- 非法状态跳转测试全部通过；
- 所有正式买点均可追溯到新鲜分钟证据；
- 杰瑞、中科回归用例通过；
- 现有合格正样本不被全部拒绝；
- 五日影子运行和二十日回放中，手续费后期望收益改善且最大单笔尾部损失下降；
- 若正式候选率显著下降或连续空仓天数明显增加，必须解释来自市场状态还是规则退化。

## 10. 回滚

- 生命周期结构可以独立回滚，不覆盖历史记录。
- 任一新退出规则在至少20个可执行样本后，若手续费后收益恶化、过早退出率明显上升或错过第一卖区的比例显著增加，则回滚为影子提示。
- 若正式候选率下降而尾部损失没有改善，回滚对应风险规则，不通过放宽全部通道补偿。
- 每次只修改一个参数组，保留前一模型版本和完整回放结果。

## 11. 实施边界

本设计仅生成分析、提示和审计，不自动下单、不登录券商、不索要账户凭据。公开行情和模型判断均可能出错，不保证盈利。

本稿通过评审后，按§8的基础设施、测试框架、状态机、公式、审计、Skill适配和原子切换顺序实施；实施时先建立失败测试，再修改策略合同、代码、Skills和自动化任务。

## 附录A：统一确定性执行合同

本附录与正文具有同等约束力。正文出现的生产名词只能使用本附录定义；Skill、模型和实施代码不得另设同名不同义规则。

### A.1 时间、分钟、VWAP与基础指标

`m1、m2、m3` 是截至决策时最近3根唯一、连续、已完成分钟，按时间先后排列；`m1`最早、`m3`最晚。三根必须属于同一连续竞价会话和同一Provider，不能跨午休、集合竞价、数据缺口或Provider切换。

```text
confirmation_window = [m1, m2, m3]
confirmation_close = close(m3)

full_day_vwap(m) =
    cumulative_amount_cny[09:30 ... m] /
    cumulative_volume_shares[09:30 ... m]

afternoon_avwap(m) =
    cumulative_amount_cny[13:00 ... m] /
    cumulative_volume_shares[13:00 ... m]

anchored_vwap(a,m) =
    cumulative_amount_cny[a ... m] /
    cumulative_volume_shares[a ... m]

TR_t = max(
    adjusted_high_t - adjusted_low_t,
    abs(adjusted_high_t - adjusted_close_(t-1)),
    abs(adjusted_low_t - adjusted_close_(t-1))
)
ATR14(seed) = mean(TR_1 ... TR_14)
ATR14(t) = (13 * ATR14(t-1) + TR_t) / 14
atr14_pct = ATR14 / adjusted_close_t
```

量统一为股、金额统一为人民币元。`amount_cny/volume_shares` 必须位于分钟高低价区间内，允许1个tick误差；否则该分钟 `unit_validation_failed`。ATR使用截至前一完成交易日的前复权数据，历史少于15个完成交易日时为 `indicator_history_insufficient`。

通道所需VWAP位置定义为：

```text
trend_vwap_reference =
    max(valid full_day_vwap, valid afternoon_avwap)

pullback_vwap_reference =
    max(valid full_day_vwap, valid afternoon_avwap)
    frozen immediately before m1

reversal_vwap_reference =
    anchored_vwap(anchor_created_at, minute immediately before m1)

vwap_position_pass =
    close(m1) >= required_vwap_reference
    and close(m2) >= required_vwap_reference
    and close(m3) >= required_vwap_reference
```

趋势延续使用 `trend_vwap_reference`，强势回踩使用 `pullback_vwap_reference`，板块反转使用 `reversal_vwap_reference`。必要VWAP为空、单位校验失败或锚点晚于m1时为 `vwap_position_data_insufficient`，不能由模型判断“看起来站稳”。

日线结构使用截至前一完成交易日的前复权数据：

```text
MA_n(t) = mean(adjusted_close[t-n+1 ... t])
return_20d = adjusted_close_t / adjusted_close_(t-20) - 1

change_t = adjusted_close_t - adjusted_close_(t-1)
gain_t = max(change_t, 0)
loss_t = max(-change_t, 0)
avg_gain_14(t) = (13 * avg_gain_14(t-1) + gain_t) / 14
avg_loss_14(t) = (13 * avg_loss_14(t-1) + loss_t) / 14
RSI14 = 100 - 100 / (1 + avg_gain_14 / avg_loss_14)

EMA_n(t) =
    2/(n+1) * adjusted_close_t
    + (1 - 2/(n+1)) * EMA_n(t-1)
DIF = EMA_12 - EMA_26
DEA = EMA_9(DIF)
MACD_histogram = 2 * (DIF - DEA)

BOLL_mid = mean(adjusted_close[t-19 ... t])
BOLL_std = population_std(adjusted_close[t-19 ... t], ddof=0)
BOLL_upper = BOLL_mid + 2 * BOLL_std
BOLL_lower = BOLL_mid - 2 * BOLL_std
boll_position =
    (adjusted_close_t - BOLL_lower) /
    (BOLL_upper - BOLL_lower)

RPS20 =
    100 * (average_ascending_rank(total_return_20d) - 1) /
    (valid_universe_count - 1)

momentum_delta_1 =
    MACD_histogram[t] - MACD_histogram[t-1]

momentum_delta_3 =
    MACD_histogram[t] - MACD_histogram[t-3]

momentum_improving =
    momentum_delta_1 > 0 and momentum_delta_3 > 0

trend_continuation_background =
    adjusted_close_t > MA11
    and MA11 >= MA23
    and RPS20 >= 70
    and momentum_improving
    and not overheated
    and not repeated_upper_shadow

strong_pullback_background =
    adjusted_close_t > MA23
    and return_20d >= 0.08
    and not repeated_upper_shadow

pullback_depth =
    pre_reclaim_low / trigger_reference - 1

pullback_boundary_pass =
    pullback_depth >= -0.015
    and pre_reclaim_low >= MA23 - 0.10 * ATR14

overheated =
    RSI14 >= 78
    or (RSI14 >= 75 and boll_position >= 0.95)
```

RSI的初始平均涨跌使用最早14个有效变化的算术平均；`avg_loss_14=0`且平均上涨大于0时RSI为100，两者均为0时为50。EMA种子为第一条有效前复权收盘。`BOLL_upper=BOLL_lower` 时 `boll_position=0.5`。

`pre_reclaim_low` 是13:00至m1前一分钟的最低价。强势回踩还必须满足 `pre_reclaim_low <= pullback_vwap_reference - tick_size`，且m1至m3收盘均不低于冻结的 `pullback_vwap_reference`。RSI和ATR至少需要15个完成交易日，BOLL和20日收益至少21日，MACD至少35日；历史、RPS宇宙或指标输入不足时为 `trend_structure_data_insufficient`，不得缩短窗口或改用自选池内部排名。

### A.2 行业同步与板块第一日反转

行业成分在09:25前冻结，只保留沪深普通A股，排除ST、`*ST`、退市整理、停牌、零成交、无有效参考价和异常交易状态证券。

```text
industry_advance_ratio =
    rising_valid_members / valid_quoted_members

industry_equal_weight_return =
    mean(last_price / exchange_reference_price - 1
         over valid_quoted_members)

industry_rank =
    unique ordinal rank by
    industry_equal_weight_return descending,
    industry_total_amount descending,
    industry_code ascending

industry_sync_pass =
    (industry_advance_ratio >= 0.50
     and industry_equal_weight_return > 0)
    or industry_rank <= ceil(valid_industry_count * 0.30)
```

行业有效成分不少于20只、实时覆盖率不低于80%才允许计算。`industry_sync_pass=false` 使趋势延续、强势回踩和高动量的机会质量从 `high` 降为 `medium`，不单独形成全市场否决。

`SECTOR_FIRST_REVERSAL_V1` 必须同时满足：

1. 使用09:25前冻结的行业成分，有效成分不少于20只、实时覆盖率不低于80%。
2. 当前行业排名不高于5，或上涨广度不低于70%且等权平均涨幅不低于2%。
3. 前一完成交易日不满足第2项，行业排名高于5，且上涨广度低于60%或等权平均涨幅低于1%。
4. 再前一完成交易日也不满足第2项，确保当前是最近3个交易日窗口内首次转强。
5. 行业排名使用“等权平均涨幅降序、行业总成交额降序、行业代码升序”的唯一顺序。
6. 个股截至决策时成交额在行业有效成分中排名前2，或位于前10%且成交额不低于10亿元。
7. 前两日快照、当日成分版本、有效数、覆盖率、广度、平均涨幅、排名和个股成交额名次全部可用。

反转锚点只能在上述事件首次成立后产生一次并冻结：时间不早于13:00；个股该完成分钟收盘突破此前10个已完成分钟最高收盘，且该分钟成交额不低于此前10个已完成分钟成交额中位数。缺失时分别为 `sector_first_reversal_data_insufficient` 或 `anchor_data_insufficient`。

### A.3 活动度、禁追价与买区

每条通道先冻结唯一 `trigger_reference`：

```text
trend_continuation:
    trigger_reference =
        max(full_day_vwap, afternoon_avwap, named_pivot)

strong_pullback_reclaim:
    trigger_reference =
        max(pullback_vwap_reference, reclaim_pivot)

sector_reversal_challenger:
    trigger_reference =
        max(reversal_vwap_reference, reversal_pivot)
```

`ACTIVITY_HOLD_V1`：

```text
confirm_amount =
    amount_cny(m1) + amount_cny(m2) + amount_cny(m3)

baseline_amount =
    median(sum of each consecutive 3-minute amount window
           within the 15 completed minutes immediately before m1)

activity_ratio = confirm_amount / baseline_amount

activity_hold =
    close(m1) >= trigger_reference
    and close(m2) >= trigger_reference
    and close(m3) >= trigger_reference
    and close(m3) >= close(m1)
    and activity_ratio >= 0.90
```

三根确认分钟量额必须均大于0，第三根不晚于该配置的入场确认截止时间。15分钟基线不足、基线为0或分钟不连续时为 `minute_data_insufficient`，不得缩短窗口。

`NO_CHASE_V1`：

```text
no_chase_price = min(
    trigger_reference * 1.015,
    confirmation_close + 0.15 * ATR14
)
```

禁追价向下按tick取整。`confirmation_close > no_chase_price` 时拒绝，原因码 `confirmation_above_no_chase`。

`BUY_ZONE_V1`：

```text
buy_zone_lower = max(
    trigger_reference,
    confirmation_close - 0.10 * ATR14
)

buy_zone_upper = min(
    no_chase_price,
    confirmation_close +
        min(0.15 * ATR14, confirmation_close * 0.005)
)
```

买区下沿向上、上沿向下按tick取整。只有正常可成交代理位于闭区间 `[buy_zone_lower, buy_zone_upper]` 才算进入买区。区间为空为 `empty_buy_zone`，价格高于上沿为 `above_buy_zone`。

### A.4 结构位、保护位、卖区与手续费后经济性

分钟摆动点使用左2根、右2根已完成分钟：

```text
minute_swing_high(i) =
    high[i] > max(high[i-2], high[i-1])
    and high[i] >= max(high[i+1], high[i+2])

minute_swing_low(i) =
    low[i] < min(low[i-2], low[i-1])
    and low[i] <= min(low[i+1], low[i+2])
```

摆动点只在 `i+2` 完成后确认。

```text
afternoon_swing_low =
    most recent confirmed minute_swing_low(i)
    where timestamp(i) >= 13:00
      and timestamp(i) < start_timestamp(m1)
      and confirmed_at(i) < start_timestamp(m1)
```

`afternoon_swing_low` 使用摆动分钟 `i` 的最低价，按 `timestamp(i)` 最近优先选择；必须与m1属于同一交易日、同一Provider，且13:00至确认时不存在未解决的数据缺口。它在m1开始前冻结，m1及之后形成或确认的低点不得回写。没有合格低点时置为 `null / afternoon_swing_low_missing`；`structural_support` 仍可从其他有效候选中选择，但所有候选均为空时保护位不可构造。

`named_pivot` 是确认窗口开始前30个已完成分钟内最近的已确认摆动高点；不存在时使用此前10个已完成分钟最高价。`reclaim_pivot` 是 `afternoon_swing_low` 对应摆动分钟之后、m1之前最近的已确认摆动高点；`reversal_pivot` 是反转锚点之后、m1之前最近的已确认摆动高点。

日线阻力使用相同的左2日、右2日确认：

```text
daily_swing_high(d) =
    high[d] > max(high[d-2], high[d-1])
    and high[d] >= max(high[d+1], high[d+2])
```

只有右侧2个交易日均已完成且在决策前可用时才确认。最近20个完成交易日内，高于买区上沿的已确认日线摆动高点与截至m3的当日高点去重后升序排列，最低为 `resistance_1`、第二低为 `resistance_2`；相同价格保留确认时间较晚的证据ID。

`PROTECTION_V1`：

```text
current_channel_pivot =
    named_pivot       if trend_continuation
    reclaim_pivot     if strong_pullback_reclaim
    reversal_pivot    if sector_reversal_challenger

structural_support =
    highest valid level strictly below buy_zone_lower
    among trigger_reference,
          current_channel_pivot,
          afternoon_swing_low

raw_protection = max(
    structural_support - 0.15 * ATR14,
    buy_zone_upper - 0.80 * ATR14
)

protection_price = min(
    raw_protection,
    buy_zone_lower - 2 * tick_size
)
```

保护位向下按tick取整。没有有效结构支撑、跨通道选择枢轴或保护位不低于买区下沿时拒绝，原因码 `protection_not_constructible`。保护位只是次日退出触发参考，不是保证成交价。

卖区按正文§5.4构造后还必须满足：

```text
first_zone_lower > buy_zone_upper
```

若 `first_zone_lower <= buy_zone_upper`，计划构造失败，原因码 `sell_zone_inside_buy_zone`，不得因为实际成交价低于上沿而放行。

价格取整方向固定为：买区下沿向上、买区上沿向下、禁追价向下、保护位向下；卖区下沿向上、卖区上沿向下。所有取整以证券元数据 `tick_size` 为准，不使用银行家舍入。

费用逐笔计算：

```text
buy_commission =
    max(buy_amount * commission_buy_rate,
        commission_min_per_order_cny)

sell_commission_i =
    max(sell_amount_i * commission_sell_rate,
        commission_min_per_order_cny)

stamp_tax_i = sell_amount_i * stamp_tax_sell_rate
buy_transfer_fee = buy_amount * transfer_fee_buy_rate
sell_transfer_fee_i = sell_amount_i * transfer_fee_sell_rate

planned_loss =
    net_loss(buy_zone_upper -> protection_price,
             including fees and slippage)

first_zone_net_reward =
    net_profit(buy_zone_upper -> first_zone_lower,
               including fees and slippage)

reward_risk_ratio =
    first_zone_net_reward / planned_loss
```

默认估算使用买卖佣金各0.03%、每笔最低5元、卖出印花税0.05%、买卖过户费各0.001%，单边滑点取5基点与1个tick中的较大者。用户费率必须生成新 `fee_policy_id`。

手续费后经济性必须同时满足 `first_zone_net_reward > 0`、`reward_risk_ratio >= 1.20`，且100股档位也通过；否则分别为 `net_reward_non_positive`、`reward_risk_below_1_20` 或 `fee_policy_missing`。

### A.5 尾部风险生产标签

以下字段只决定 `tail_risk`，不得被模型自由解释：

```text
high_atr_warning =
    atr14_pct >= 0.04
    or atr14_pct >= cross_sectional_percentile_80

overnight_gap(d) =
    abs(adjusted_open_d / adjusted_close_(d-1) - 1)

overnight_gap_warning =
    percentile_90(overnight_gap, latest 60 completed sessions) >= 0.04

upper_shadow_ratio =
    0 if adjusted_high = adjusted_low
    else (adjusted_high - max(adjusted_open, adjusted_close)) /
         (adjusted_high - adjusted_low)

long_upper_shadow =
    upper_shadow_ratio >= 0.35

repeated_upper_shadow =
    count(long_upper_shadow in latest 3 completed sessions) >= 2

industry_crowding_warning =
    industry_advance_ratio >= 0.85
    and industry_equal_weight_return >= 0.04
    and industry_amount /
        median(industry_amount, latest 20 completed sessions) >= 1.50

distance_to_up_limit_pct =
    (official_up_limit_price - latest_executable_price) /
    latest_executable_price

distance_to_down_limit_pct =
    (latest_executable_price - official_down_limit_price) /
    latest_executable_price

limit_proximity_warning =
    distance_to_up_limit_pct <= max(0.01, 0.25 * atr14_pct)
    or distance_to_down_limit_pct <= max(0.02, 0.50 * atr14_pct)
```

横截面ATR百分位使用对应 `strategy_profile_id` 的09:25冻结合格宇宙，覆盖率低于95%时百分位项为空，但绝对4%阈值仍可计算。隔夜样本少于60日时为 `overnight_gap_history_insufficient`，不能把缺失当成无风险。

涨跌停价优先使用交易所或已登记Provider给出的当日官方值；缺失时按交易所参考价与证券当日 `instrument_limit_rate` 计算，并分别按交易所涨停、跌停方向取整。参考价、限制比例或特殊交易状态无法核验时为 `limit_price_data_insufficient`，不得套用默认10%。

- 单一 `high_atr_warning`、`overnight_gap_warning`、`long_upper_shadow`、`industry_crowding_warning`、`limit_proximity_warning` 或一手不可拆分只把尾部风险提高到 `medium`，不单独否决。
- `repeated_upper_shadow=true` 对趋势延续和强势回踩属于机会质量硬失败，原因码 `terminal_distribution_risk`。
- 任一账户硬预算、市场硬否决、保护位或正常退出可成交性失败时，尾部风险为 `high`。

`high_momentum` 在 `HIGH_MOMENTUM_V1` 的完整背景、确认和回归合同写入本文件前固定为 `research_only / production_buyable=false`；不得仅凭“高流动性、强势”自然语言进入生产。

### A.6 正常可成交代理、开盘代理与退出意图

`EXECUTABILITY_V1` 将市场理论可执行与用户实际成交分账：

```text
market_executability =
    executable / unavailable / ambiguous / data_insufficient

user_execution =
    user_reported_executed /
    user_reported_not_executed /
    execution_unknown
```

“正常可成交窗口”是同时满足以下全部条件的完成分钟；不等同于“有成交量的任意分钟”：

1. 位于连续竞价时段 `[09:30,11:30)` 或 `[13:00,14:57)`；集合竞价不作为理论成交代理。
2. 证券状态正常，不是停牌、临时停牌、退市整理、无涨跌幅限制异常日或来源状态冲突。
3. `volume_shares >= proxy_required_quantity`、`amount_cny > 0`，OHLC和标准VWAP均在合法价格范围；`plan_only` 的 `proxy_required_quantity=100`，`account_aware` 使用计划数量。
4. 买入分钟不是 `open=high=low=close=涨停价`，卖出分钟不是 `open=high=low=close=跌停价`。
5. 若上一分钟仍是不利方向一字板，瞬时开板不构成窗口；必须连续两根完成分钟均有成交，且至少一根收盘距离不利方向涨跌停价不少于1个tick，第二根结束才恢复。

有可验证一档盘口时，买入代理使用卖一价和卖一可见量，卖出代理使用买一价和买一可见量。只有OHLCV时：

```text
bar_vwap = amount_cny / volume_shares
buy_proxy_fill = min(high, max(signal_price, bar_vwap))
sell_proxy_fill = max(low, min(signal_price, bar_vwap))
```

代理价必须位于分钟 `[low,high]`，否则 `proxy_price_invalid`。

开盘术语固定为：

```text
opening_reference_price =
    valid exchange official open
    or first valid transaction price at/after 09:30

first_normal_executable_window =
    earliest completed minute at/after 09:30
    satisfying all EXECUTABILITY_V1 conditions for selling

open_proxy_price =
    verified best_bid at opening when visible quantity is sufficient
    else sell_proxy_fill(first_normal_executable_window,
                         signal_price=opening_reference_price)

open_validation_end =
    earliest of:
      close of the third consecutive normal executable minute
          beginning with first_normal_executable_window,
      09:40:00,
      a terminal full-exit event
```

若官方开盘价缺失且第一笔有效成交也缺失，或截至09:40仍没有正常可成交窗口，分别进入 `opening_reference_data_insufficient` 或 `exit_unavailable`，不得用昨收、最新一次旧报价或分钟最高价替代。

“未完成退出意图”定义为已经生成不可变 `exit_intent_id`，但意图数量尚未全部获得已对账成交或已对账未执行结论：

```text
unresolved_exit_intent =
    requested_exit_quantity > reconciled_executed_quantity
    and user_execution in {execution_unknown,
                           user_reported_not_executed}
    or market_executability in {unavailable,
                                ambiguous,
                                data_insufficient}
```

每个退出意图保存证券、数量、原因、触发价、`created_at`、首次正常窗口、所有尝试、市场可执行状态和对账状态。后续意图不能覆盖前一意图。

### A.7 卖出执行、开盘早期反转与事件优先级

`EXIT_EXECUTION_V1`：

```text
first_zone_touch =
    executable minute high >= first_zone_lower

first_zone_confirmed =
    two consecutive completed minute closes >= first_zone_lower
    and second close >= first close

exit_activity_ratio =
    amount of latest 3 completed minutes /
    median(3-minute amount windows in prior 15 completed minutes)

extension_allowed =
    first_zone_confirmed
    and exit_activity_ratio >= 0.90
    and latest two closes >= next_day_full_day_vwap
```

- 一手仓位首次理论可执行触及第一卖区即全部退出。
- 可拆分仓位首次触及第一卖区兑现50%；只有 `extension_allowed=true` 才保留剩余部分等待第二卖区。
- 第二卖区首次理论可执行触发时退出全部剩余仓位。
- 第一卖区兑现后，任一后续完成分钟收盘跌破第一卖区下沿或次日全天VWAP，在下一正常可成交窗口退出剩余部分。
- 活动度历史不足18根、基线为0或量额缺失时 `extension_allowed=false / exit_activity_insufficient`，不阻止一手或第一卖区基础退出。

开盘场景使用附录A.6的 `open_proxy_price`，按互斥区间判定：

1. `open_proxy_price >= second_zone_lower`：第一正常可成交窗口全部退出。
2. `first_zone_lower <= open_proxy_price < second_zone_lower`：执行第一卖区数量规则。
3. `protection_price < open_proxy_price < first_zone_lower`：进入开盘验证。
4. `open_proxy_price <= protection_price`：第一正常可成交窗口提交全部退出；没有正常窗口则 `exit_unavailable`。

保护位成交边界：

```text
protection_fill_band =
    max(2 * tick_size, 0.10 * ATR14)

normal_protection_fill =
    sell_proxy_fill >= protection_price - protection_fill_band

gap_or_slippage_overshoot =
    sell_proxy_fill < protection_price - protection_fill_band
```

接近目标的到达方式必须分离：

```text
target_activation_price =
    entry_price
    + target_progress_activation
      * (first_zone_lower - entry_price)

target_arrival_mode =
    gap_arrival
        if open_proxy_price >= target_activation_price
    intraday_runup
        if open_proxy_price < target_activation_price
        and first later executable price crosses
            target_activation_price from below

activation_at =
    first executable timestamp satisfying the selected mode

runup_high_since_activation =
    maximum executable price observed from activation_at
```

禁止使用收盘后回看的 `session_high_since_open` 反向认定激活。股票开盘已经位于激活价上方属于 `gap_arrival`，不冒充日内冲高；日内模式必须从下向上首次穿越并冻结 `activation_at`。

一般开盘失败定义为：

```text
failed_open_structure =
    before or at open_validation_end
    and two consecutive completed executable minutes
        each close below at least two of:
          opening_reference_price,
          next_day_full_day_vwap,
          frozen named_pivot
    and neither minute makes a new executable high
```

`named_pivot` 对持仓使用买入计划冻结值，不在次日重选。缺少其中一个参考位时仍可用另外两个判断；少于两个有效参考位时为 `failed_open_reference_data_insufficient`，不能触发该分支，但结构保护位和时间退出仍有效。

`early_target_reversal` 用于覆盖开盘5至10分钟内的杰瑞股份式冲高回落，不依赖18根活动度历史：

```text
early_target_reversal =
    activation_at <= open_validation_end
    and two consecutive completed executable minutes
        both close below opening_reference_price
    and both close below next_day_full_day_vwap
    and neither minute makes a new executable high
```

触发后进入 `failed_open_exit`，在下一正常可成交窗口退出；它是确定性开盘结构规则，不等待利润保护参数完成校准。

`EXIT_EVENT_PRIORITY_V1`：

1. 先判定市场可执行性；没有正常窗口只能记录 `exit_unavailable`，不能记录成交。
2. 有逐笔或更细时间戳时，互斥事件按首次 `available_at` 的真实先后顺序。
3. 只有分钟OHLCV且保护位与卖区同分钟触发时，标记 `path_order_ambiguous`，风险审计按保护位先发生，概率训练排除顺序标签。
4. 开盘验证结束前，弱势反转统一归入 `failed_open_exit`；`profit_protection` 可以计算影子证据，但不得与其竞争状态。
5. 开盘验证结束后，已先触及第一/第二卖区则执行 `target_exit`；未触及卖区而后满足利润保护才进入 `profit_protection`。
6. `structural_stop_exit` 高于 `failed_open_exit/profit_protection/time_exit`；`time_exit` 只在此前均未触发且14:30仍有仓位时发生。
