interface:
  display_name: "MarketBase"
  short_description: "Collect objective A-share market data and audit evidence"
  default_prompt: "Use $marketbase to collect A-share source data or fulfill a validated data request."

policy:
  allow_implicit_invocation: true
