# T1 云端策略总纲

> **策略在此，不在本地代码。**
> 本文档是 T+1 策略在云端层的唯一规则源。本地代码只提供事实与计算，不拥有策略解释权。

| 项 | 值 |
|---|---|
| 文档版本 | cloud-v2.0 |
| 生效日期 | 2026-08-27（收盘后生效，取代 cloud-v1.0） |
| 状态 | 生效 |
| 适用市场 | 沪深 A 股（T+1 制度） |
| 时区 | Asia/Shanghai |
| 事实来源（工具） | `reports/全盘T1策略扫描报告_*.html`、`data/cache/fast/scan_result_*.csv`、`data/cache/fast/snapshot_latest.csv` |
| 事实来源（用户） | `strategies/portfolio_state.json`（持仓台账与成交对账，见 §10） |
| 影子机制依据 | `V07301026_A股全生命周期策略优化设计.md`、`MODEL_VERSION.md`、`strategies/strategy_lifecycle/*`（仅登记，见 §15） |
| 对应本地工具 | `fast_t1_scan.py`（主）、`marketbase` 管道、`local_workflow.py` |

---

## 0. 权威声明与裁决顺序

"策略在此，不在本地代码"的操作含义：

1. **裁决规则以本文档为唯一依据**：买不买、买什么、什么价买、买多少、何时退、破线怎么办、异常怎么处理，全部由本文档定义。
2. **本地代码是工具层**：它采集快照、计算指标、产出扫描报告。其输出的数值（现价、MA、RSI、ATR、量比、评分、买区、保护位……）是**事实**，云端必须原样引用，禁止重算、改写或"看起来不对就修一下"。
3. **两类事实，一个裁决源**：工具事实（扫描输出）与用户事实（成交报告、持仓截图、明确表态，经 §10 台账对账）都是输入；对它们的**解释与裁决**只属于本文档。
4. **冲突裁决顺序**：
   1. 事实数值 → 以本地扫描输出为准（云端不改算）；
   2. 规则、阈值、纪律、状态含义 → 以本文档为准（代码不解释策略）；
   3. 本文档未覆盖的情形 → 取最保守路径（不入场、退出优先），并在当日输出中记录"待裁决事项"。
5. **修改策略 = 修改本文档并升版本**（见 §16）。任何代码升级、重构、提速不得单方面改变策略含义；若工具输出的口径变化导致数值含义改变，必须先在本文档登记再使用。
6. **与本地 2.0.0-dev 生命周期工程的关系**：该工程描述的是本地实现路线。其已生效的确定性口径（指标计算、排除当日 bar 等）作为事实标准被本文档吸收；其开发中/影子状态的机制统一登记于 §15 影子机制登记簿，验收前**不构成生产规则**，不得引用。
7. 本总纲取代旧适配合约中"判在本地"的定位：本地继续管"算"，"判"的规则源与持仓账本（§10）都在云端本文档。

## 1. 策略定位

- **形态**：A 股 T+1 隔夜纯短线。当日筛选 → 尾盘/回踩入场 → 次日退出 → 最迟次日 14:30 提交全部剩余退出意图。
- **不跨第二个夜晚**：失败交易不得改称波段持有。
- **不制造长期空仓，也不放松门槛**：市场正常时保留正常出手机会；环境恶化时用环境模式收紧（§4），不叠加临时否决。
- **空仓是合法状态**：无合格候选时必须明确输出"空仓"，不得为降低连续空仓天数凑数。
- **每日配额为上限而非任务**：正式候选 ≤20、条件观察 ≤15（§7），填不满就留空。
- **持仓优先于新候选**：台账（§10）存在未平仓持仓时，先给持仓的退出管理，再谈新入场。

## 2. 分层契约：本地管"算"，云端管"判"，台账记"实"

### 2.1 职责划分

| 层 | 职责 | 禁止 |
|---|---|---|
| 本地工具层 | 快照采集、日线指标（MA/RSI/MACD/ATR/BOLL）、量比、扫描过滤、评分与区间计算、报告落盘 | 持有策略解释权；读写用户台账 |
| 用户事实层（台账） | 记录用户成交报告、持仓证据、明确表态，承载对账状态（§10） | 由本地工具改写；把理论值伪装成用户实际成交 |
| 云端策略层（本文档） | 规则裁决、候选晋级、次日计划生成、退出协议、风险纪律、审计归因、用户文案、台账维护 | 改算数值、无数据推断、绕过禁追线话术 |

### 2.2 云端五不做（硬红线）

1. 不改算本地输出的任何数值；
2. 不在 `data_insufficient` 时用记忆、旧报告或"常识"补值；
3. 不把条件观察表述为可执行买点（"可以买""挂单价"禁用）；
4. 不给出任何越过禁追线的追价建议；
5. 不用自然语言覆盖市场环境模式收紧后的门槛。

### 2.3 日常运行手册

| 时点 | 动作 | 命令（项目根目录） |
|---|---|---|
| 盘中（可选） | 快速扫描 | `python fast_t1_scan.py`（快照 15 分钟内自动复用） |
| 收盘后 | 收盘扫描 + 云端裁决 + 台账复核 | `python fast_t1_scan.py --fresh 0`（强制新采）→ 按本总纲生成次日计划 → 按 §10 更新台账 |
| 次日盘中 | 退出协议执行 / 实时复核 | `python fast_t1_scan.py --realtime <代码,代码>`（单源失败处置见 §12） |
| 数据地基（按需） | 官方采集与对账 | `python local_workflow.py collect` |

盘中/午间/收盘三档相位由工具按运行时刻自动标注；收盘后运行即为收盘口径（当日 bar 已补入）。

## 3. 股票宇宙与硬过滤

宇宙：沪深 A 股全市场（北交所不参与策略，仅在报告中跟踪覆盖审计）。

每日扫描按以下顺序硬过滤，任一不过即出局，**不做豁免**：

| # | 过滤项 | 条件 |
|---|---|---|
| 1 | 风险警示 | 剔除 ST / *ST / 退市风险标记；名称含 "ST" 或 "退" 一并剔除 |
| 2 | 停牌 | 剔除停牌标记 |
| 3 | 涨跌停 | 当日涨跌幅在 (-9.9%, +9.9%) 之外剔除（近似一字板无法次日从容退出） |
| 4 | 市场 | 剔除北交所 |
| 5 | 成交 | 当日成交量 ≤ 0 剔除 |
| 6 | 次新 | 上市不足 60 个自然日剔除（字段缺失时不以此过滤） |
| 7 | 流动性-成交额 | 当日成交额 ≥ 5000 万元 |
| 8 | 流动性-市值 | 流通市值 ≥ 30 亿元 |
| 9 | 流动性-换手 | 换手率 ∈ [0.5%, 15%] |
| 10 | 指标完备 | MA5/10/20/60、RSI14、ATR%、BOLL 位置任一缺失即出局（不猜） |
| 11 | 趋势前置 | 必须 MA5 > MA10 > MA20（趋势未成多头直接出局） |
| 12 | 板块（v2.1） | 剔除创业板（代码 30 开头）与科创板（代码 68 开头），仅沪主板（60）/深主板（00）参与 |
| 13 | 价格（v2.1） | 现价 > 50 元，否则剔除 |

指标口径：基于官方日线缓存、**排除当日 bar**（与官方管道一致），ATR/RSI/BOLL 均为该口径下的冻结值。

## 4. 市场环境模式（动态门槛）

**样本基（工具冻结口径，云端不得改用其他样本重算）**：上涨占比与涨跌中位数以「§3 第 1–6 项硬过滤完成后、第 7–9 项流动性过滤之前」的全市场样本计算（当日有效涨跌幅非空者）。行业平均涨幅（§5.1）在同一样本基上按行业分组取简单算术平均。

| 上涨占比 | 模式 | 评分门槛 | 尾部风险标签上限 |
|---|---|---|---|
| < 45% | 弱市模式 | 45 | 1 |
| > 60% | 强势模式 | 40 | 2 |
| 其余 | 正常模式 | 40 | 1 |

- 模式只调整"评分门槛 + 尾部风险容忍"两项，不新增一票否决；
- 弱市收紧、强势放宽，均由本表唯一决定，禁止临场加码；
- 每次扫描冻结当时点的模式；同一交易日多次扫描各自冻结，**收盘扫描的模式与数值为该日最终口径**，盘中扫描结果仅为过程快照；
- 市场环境数据不足时按弱市模式处理（最保守路径）。

## 5. 双轴判断：机会评分 × 尾部风险

### 5.1 机会评分（0–90，工具已算，云端只引用）

| 维度 | 条件 | 分值 |
|---|---|---|
| 趋势 | MA5>MA10>MA20>MA60 完整多头 | +20 |
| 趋势 | 仅 MA5>MA10>MA20 | +10 |
| RSI14 | 45–65（健康区） | +15 |
| RSI14 | 40–70 其余区间 | +8 |
| 量比 | 1.2–3.0（温和放量） | +15 |
| 量比 | 0.8–1.2（常量） | +5 |
| 当日涨幅 | +0.5% ~ +5.0% | +10 |
| 当日涨幅 | -1.0% ~ +0.5% | +5 |
| VWAP 位置 | 现价 > 当日成交额/成交量（站上均价） | +10 |
| 行业 | 行业成分当日平均涨幅 > 0 | +10 |
| 20 日收益 | > 0 | +10 |
| 20 日收益 | > -5% | +5 |

量比口径：当日累计成交量 ÷ 前 5 个完整交易日均量，盘中按已交易分钟数折算（收盘运行为全天口径）。

### 5.2 尾部风险标签（计数制，工具已算）

| 标签 | 触发条件 |
|---|---|
| high_atr | ATR14 / 现价 > 6% |
| boll_overbought | BOLL 位置 > 0.95 |
| high_turnover | 换手率 > 10% |
| long_upper_shadow | 上影线占比 > 0.5 |

### 5.3 双轴合取（不可互相抵消）

候选资格 = 评分 ≥ 环境门槛 **且** 尾部标签数 ≤ 环境上限 **且** RR ≥ 1.5 **且** 行业当日平均涨幅 > 0。

- 禁止"高分覆盖高风险"或"低风险抬升低分"；
- 正式候选另加两条硬条件：评分 ≥ 55 且尾部风险标签 = 0（§7）。

## 6. 价格合同：买区 / 禁追线 / 保护位 / 卖区

全部由工具按冻结公式计算（ATR 缺失时以现价 × 2% 代入），云端**只引用不改写**：

```text
买区     = [ max(现价 − 0.5×ATR14, BOLL下轨),  现价 − 0.1×ATR14 ]
禁追线   = (买区上沿 + 0.3×ATR14) × 1.005        # 含 0.5% 容差
保护位   = 买区下沿 − 0.5×ATR14
卖区一   = [ BOLL上轨 × 0.99,  BOLL上轨 ]
卖区二   = [ 卖一上沿 + 0.3×ATR14,  卖一上沿 + 0.8×ATR14 ]
RR     = (卖一下沿 − 买区上沿) ÷ (买区上沿 − 保护位)   # 候选要求 ≥ 1.5
```

- 买区是**回踩承接区**，不是突破追入区；只有价格回到买区内才谈入场。
- 禁追线是硬边界：价格在禁追线之上时，无论评分多高一律"不追"。
- 保护位是次日退出触发参考，不是保证成交价；破位后无条件执行（§9）。

## 7. 候选晋级与输出配额

```text
候选池  ：通过 §3 过滤 + §5.3 合取，按评分降序
正式候选：候选池中 评分≥55 且 尾部标签=0，取前 20
快速介入：正式候选中 评分≥85，标注"快速介入"
条件观察：候选池其余部分，取前 15
```

- 快速介入的含义：可不等待深度回踩、在买区上沿附近即可介入，**仍不得越过禁追线**；
- 条件观察必须给出"确认触发 + 失效条件 + 下一次检查时间"，不得直接消失，也不得写成买点；
- 所有配额是上限；空缺不补足。

## 8. 执行纪律（每日输出必须原文携带）

1. 仅在价格回踩**买区**时挂限价单介入，**禁追线**为硬边界，突破不追；
2. 评分 ≥85 标记"快速介入"的标的可不等深度回踩（仍不超过禁追线）；
3. 尾盘 14:50 仍未进入买区则放弃当日该标的；
4. 单只仓位 ≤8%，组合合计 ≤30%；跌破保护位无条件离场（T+1 次日执行）；
5. 本报告为量化筛选结果，不构成投资建议。

## 9. T+1 次日退出协议

持仓生命周期：`入场 → T+1锁定（当日不可售） → 次日开盘验证 → 盘中监控 → 退出 → 关闭`。

### 9.1 次日开盘四情景（互斥，先判卖二再判卖一）

| 情景 | 条件 | 动作 |
|---|---|---|
| 强势高开 | 开盘价 ≥ 卖二下沿 | 第一正常可成交窗口**全部退出** |
| 达第一目标 | 卖一下沿 ≤ 开盘价 < 卖二下沿 | 一手仓位全部退出；可拆分仓位兑现 50%，仅强势延续才保留等卖二 |
| 正常开盘 | 保护位 < 开盘价 < 卖一下沿 | 进入开盘验证（约至 09:40）：冲近卖一后连续走弱（跌破开盘价/VWAP）→ 失败开盘退出 |
| 弱势低开 | 开盘价 ≤ 保护位 | 第一正常可成交窗口**全部退出**；无法成交 → exit_unavailable |

### 9.2 盘中监控（开盘验证后至 14:30）

- 触及卖一：一手全退；可拆分兑现 50%；
- 触及卖二：剩余全部退出；
- 13:00 后跌破保护位：立即退出，不等下一复盘节点；
- 全天平淡未触卖区未破保护：**14:30 时间退出兜底**，提交全部剩余退出意图；
- 跌停/停牌无法成交：标记 `exit_unavailable`，后续每个交易日从第一正常可成交窗口继续退出监控，**风险线不放宽**，不得静默移出持仓列表。

### 9.3 退出铁律

- 一手（100 股）仓位：首次触及卖一即全部退出，不为等卖二放弃已出现的正常退出窗口；
- 理论可执行 ≠ 实际成交：触发记录的是"退出意图"，用户实际成交单独对账（§10）；
- 禁止对冲式加仓（不因另一持仓亏损向同标的或相关标的加仓摊成本）；
- 用户明确拒绝退出 → 记为用户偏离持有，只继续给风险退出建议，不恢复原盈利目标；
- 用户未反馈执行情况 → 执行状态未知，不推定已空仓或已退出。

## 10. 持仓台账与成交对账（cloud-v2.0 新增）

持仓账本是云端策略层的组成部分：规则在本文档，状态记于台账文件 `strategies/portfolio_state.json`。本地工具不得读写该文件。

### 10.1 三账分离

三套结果永远分账，不得互相冒充：

```text
市场理论路径  ：按 §6/§9 由工具数值推演的退出意图与理论价格
用户报告成交  ：用户口头/文字报告的成交（先记 unverified）
券商证据      ：持仓截图、成交记录（screenshot_verified / broker_record_verified）
```

### 10.2 台账结构（事实字段）

- `account.positions[]`：代码、名称、市场、行业、数量、可售数量、成本、状态（`intraday_position_monitor` 等 §9 生命周期状态）、保护位、当前有效 `exit_plan`（含 as_of 与有效交易日）、证据来源、账本时间；
- `execution_ledger[]`：每条成交至少含 `execution_id`、代码、方向、数量、价格、报告时间、推定成交日、证据来源、对账状态、盈亏状态、备注；
- `strategy_policy`：已生效的生产政策（见 10.5）。

### 10.3 对账状态机

```text
unverified → screenshot_verified / broker_record_verified
任意状态冲突 → conflict_pending
用户更正     → 追加 correction 记录，不覆盖原报告与状态历史
```

- 数量守恒：`昨日数量 + 买入 − 卖出 − 转出 + 转入 = 当前数量`；数量、成本或可售数冲突时进入 `conflict_pending`，不得强行平账；
- 只有口头报告时可临时更新持仓视图，但审计必须标明未对账。

### 10.4 执行状态与持续管理义务

- 用户未反馈 → `execution_unknown`：不推定空仓、不推定已退出；下一份权威持仓证据到达时补做对账；
- 用户明确拒绝退出 → `user_deviation_hold`：只给风险退出建议，不恢复原盈利目标，不计入策略按计划收益；
- 台账数量 > 0 的持仓，**每个交易日**必须更新现价、可售数量、保护位状态与下一退出动作，直至 `closed` 或对账证明持仓不存在；不因 T+N 期限静默放弃管理；
- `exit_plan` 是当前有效的冻结快照：改动作须整体替换并更新 as_of，不得原地局部涂改含义。

### 10.5 生产政策（已在台账冻结）

| 政策 | 内容 |
|---|---|
| single_lot_exit_guard | 一手持仓且成本未对账确认时，**不输出固定盈利卖点**；仅输出风险线（保护位/硬止损）与待确认信息；盈利目标必须 ≥ 手续费后保本价方可发布 |

新增或修改生产政策按 §16 规则升版本，不得临场创造。

## 11. 主题叠加信号：BYD-半导体轮动

- **触发**：比亚迪（002594）**收盘**涨跌幅 ≤ −1%；判定以收盘扫描为准，盘中扫描显示的信号状态为临时预览，不单独作为次日叙述依据；
- **含义**：次日半导体板块轮动关注信号，历史胜率 73.3%（33/45）；
- **未触发**：次日半导体无轮动加持，相关持仓按既定纪律执行；
- **边界**：信号只是叠加提示——可提高半导体个股的叙述优先级，**不改变 §3–§10 任何硬合同，不单独构成买入理由**；比亚迪不在快照中时信号状态未知，如实标注。

## 12. 数据不足与异常处理

| 情形 | 处理 |
|---|---|
| 扫描失败 / 快照过期 / 报告缺失 | 当日输出"数据不足，保持空仓"，禁止用旧报告或记忆补值 |
| 个股指标缺失 | 该股出局，不猜测、不降档使用 |
| 实时查询个别代码失败 | 明确标记"未找到/获取失败"，不影响其余代码结果 |
| 实时链路单源失败（如 sina 空返回） | 该链路结果整体标记获取失败；禁止用旧快照价格充当实时价；必要时以其他已登记事实链路（如腾讯个股行情）单独重取，引用新值并注明来源与时间 |
| 北交所链路失败 | 只标记失败（北交所本就不参与策略），不影响沪深 |
| 盘后重建数据 | 仅用于诊断，不得改变当日已输出决策 |

## 13. 输出合同（每日报告结构）

云端每日产出按固定顺序，缺项必须显式标注而非省略：

1. **市场环境**：上涨占比、涨跌中位数、环境模式、筛选漏斗（总数→硬过滤→候选→正式）；
2. **行业强弱**：领涨前 5、领跌后 5（当日平均涨幅口径）；
3. **BYD-半导体信号**：触发/未触发 + 一句话含义；
4. **持仓状态**（台账存在未平仓持仓时必输出）：代码、数量、成本（或未对账标记）、现价、保护位、下一退出动作、对账状态；
5. **TOP 5 精选**：现价、涨跌、评分、RR、买区、保护位、禁追线、卖一、行业、标签；
6. **正式候选**（≤20，含"快速介入"标注）；
7. **条件观察**（≤15，含确认触发与失效条件）；
8. **执行纪律**五条（§8 原文）；
9. **次日退出计划**（对拟入场标的按 §9 给出四情景预案）；
10. **数据溯源**：快照时间与来源、指标耗时、报告路径、台账 as_of；
11. **风险声明**。

## 14. 审计与失败归因

每笔已完成交易归入且仅归入一类：

| 类别 | 含义 |
|---|---|
| selection_failure | 选错股票（行业/趋势/评分判断失误） |
| entry_failure | 买错位置（追价、买区外入场、确认不足） |
| exit_failure | 退出过晚（目标刚性、破位拖延、时间退出未执行） |
| execution_failure | 无法成交（跌停、流动性、用户未执行） |

跟踪指标：正式候选率、条件观察转化率、退出执行率、手续费后收益、最大单笔尾部损失、连续空仓天数；台账驱动的对账指标：成交对账完成率、`execution_unknown` 存量天数、用户偏离持有笔数、风险线失守至实际退出的延迟。

## 15. 影子机制登记簿（cloud-v2.0 新增）

以下机制存在于本地设计稿/代码中，但**验收前不构成生产规则**：不得出现在每日输出的可执行建议里，不得被话术引用，其产出仅可用于影子对比与审计。吸收须按 §16 升次版本。

| 机制 | 定义来源 | 当前状态 | 生产吸收门槛 |
|---|---|---|---|
| 利润保护网格（target_progress=0.80、回撤 0.50×ATR、确认 2 分钟、活动度 ≤0.65 为影子默认参数） | 设计稿 §5.3 / `profit_protection.py`（shadow_mode） | 影子 | ≥60 可执行样本、覆盖 ≥3 通道、每通道 ≥10 样本、≥20 交易日，走步验证含费用与不可成交路径；影子运行 ≥5 个交易日 |
| 盘中市场否决 INTRADAY_MARKET_VETO_V2（四指数恶化 ≥3 + 宽度 ≤35% / 成长双指数否决） | 设计稿 §3.4 | 未接入生产工具 | 影子 ≥5 交易日；中科曙光买入日回放须能触发对应否决 |
| 入场状态机与分钟确认（3 分钟确认、ACTIVITY_HOLD_V1、分钟版买区/禁追价） | 设计稿 §4 / 附录 A.3 / `strategy_lifecycle/` | 开发中 | 设计稿 §8.1 实施顺序完成 + 影子 ≥5 交易日 |
| high_momentum / sector_reversal_challenger 通道 | `MODEL_VERSION.md` | 影子（production_buyable=false） | 合同写入设计稿 + 各自影子样本门槛 |
| ACCOUNT_RISK_V1 账户风险预算（0.25%/0.50%/0.90%/1.00% 等压力上限） | 设计稿 §3.2 | 开发中 | ACCOUNT_SNAPSHOT_V1 链路可用并通过 §8.3 独立验证 |
| RPS20 全市场相对强弱 | 设计稿 附录 A.1 | 开发中 | 合格宇宙覆盖率 ≥95% 验证 |

边界说明：cloud-v1.0 已吸收为生产的确定性口径（指标冻结、退出协议、`exit_unavailable`、用户偏离持有、`execution_unknown` 等）继续有效，本登记簿不回溯撤销；登记簿机制之间也不得互相拼接成"准生产"话术。

## 16. 版本与变更规则

- 当前版本 **cloud-v2.0**（2026-08-27 生效）；
- **数值变更**（阈值、区间、分值）：须有 ≥3 个同类问题交易日且 ≥20 个可执行样本支撑，一次只改一个参数组，并记录前后对比；
- **规则变更**（过滤、状态、退出路径）：须升次版本号并附触发原因与回放证据；
- 勘误（章节引用修正、口径登记）可在次版本一并处理，不属于数值变更；
- 禁止因单日、单股结果临时改参数；盘中不出新版本；
- §15 登记簿机制的验收通过后，按本节流程升版吸收，不自动生效；
- 变更日志追加于文末，不覆盖历史。

### 变更日志

| 版本 | 日期 | 变更 |
|---|---|---|
| cloud-v1.0 | 2026-08-27 | 创立总纲：从 fast_t1_scan.py 生产口径 + 生命周期设计稿中固化云端规则源 |
| cloud-v2.0 | 2026-08-27 | ① 新增 §10 持仓台账与成交对账合同（三账分离、对账状态机、single_lot_exit_guard 收编为成文政策）；② 新增 §15 影子机制登记簿，取代 v1.0 对 2.0.0-dev 的笼统排除；③ 修正 §4 环境口径样本基描述（实为硬过滤后、流动性过滤前，经 20260827 快照数值核验 61.331%↔报告 61.3%）；④ 登记 BYD 信号以收盘扫描为准、盘中为预览，触发表述由"收盘跌幅 ≤ −1%"修正为与代码判定一致的"收盘涨跌幅 ≤ −1%"；⑤ §12 增补实时链路单源失败处置（当日实测 sina 空返回）；⑥ 输出合同新增"持仓状态"节并确立持仓优先于新候选；⑦ 勘误 v1.0 四处错误章节引用（§0.4→§16、§1→§7、§5.3→§7、§6→§9）。未调整任何数值阈值与过滤条件 |
| cloud-v2.1 | 2026-09-03 | §3 新增两条硬过滤（用户直接指令）：#12 剔除创业板（30 开头）与科创板（68 开头），仅沪主板（60）/深主板（00）参与；#13 现价 > 50 元。触发原因：用户明确要求推荐范围收窄为主板高价股。注意：该变更当日即时生效，未走 §16"数值变更须 3 交易日证据"流程——属用户指令型规则变更（板块/价格过滤为用户偏好约束，非策略参数调优）；当日 9/3 TOP 5（海油工程 6.12 / 金田股份 13.69 / 宁波中百 19.15 / 立霸股份 15.88 / 博汇股份 12.73，全部 <50 元）全部不符合新规则，9/4 起推荐口径将显著变化 |

## 17. 风险声明

本策略为量化筛选与决策支持研究，不构成投资建议，不承诺盈利，不自动下单、不登录券商、不索取账户凭据。公开行情可能延迟、缺失、限流或口径不一致；A 股 T+1、涨跌停、停牌、流动性与隔夜跳空可能使实际损失显著超过计划保护位。市场有风险，投资需谨慎。
